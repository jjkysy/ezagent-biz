# 00 · 总览（df-prd 项目导读）

> 这套文档（01–06）讨论的是同一个产品：**在 ezagent 上搭一个"团队产品开发 workspace"**。本文是导读——给一句话定位、规定阅读顺序、把"已经确定的结论"和"还没验证/还没拍板的事"分开摆，最后给下一步行动。
>
> 全程大白话。引 ezagent 代码带 file 路径（相对 worktree 根 `/home/yaosh/projects/ezagent-biz/.claude/worktrees/ezagent-yao/`）；引外部工具/方法论带可核查链接。

---

## 一句话定位

一个**"思维导图当骨架、每人一个 agent 当神经、产物自动挂回节点当血肉"的团队产品开发 workspace**——它把"想清楚（战略）→ 做出来（开发）→ 卖出去（运营）"整条逻辑链收进一张活的思维导图，每个节点有人认领、产物自动挂回、每人一个 agent 替你盯、一个网页出口让全队无会对齐；它同时是开发 ezagent 主产线时自己用的 dogfooding 工具。底座 = ezagent（Elixir/OTP 消息路由 + 多 agent 编排运行时）。

差异化一句话：别人（Linear / Notion / 飞书项目）是"任务工具 + AI 插件 + 手动贴链接"，我们是"思维导图当骨架 + 每人一个长期 agent 当神经 + 产物自动挂载当血肉"。

---

## 读这套文档的顺序

六篇是一条从"想法"到"能开工 + 能评估"的链，**建议顺序读，每篇一句话说清它解决什么**：

| 顺序 | 文档 | 它回答什么 | 读完你能 |
|---|---|---|---|
| 1 | **03-思维导图.md** | 产品第一张大图：定位→痛点→用户→体验→功能→具体开发→运营→ROI→节奏，9 个主干层层下钻到可开工叶节点（含缩进列表 + mermaid 两版） | 看懂整个产品的逻辑链骨架（**先读这篇拿全局**） |
| 2 | **01-工具选型.md** | 每个诉求点该用哪个工具、为什么、怎么跟 ezagent 接、风险/替代（markmap / excalidraw / GitHub / 飞书 / SQLite / Entity.Agent / socialware） | 知道每块用什么工具、真相源在哪 |
| 3 | **04-spec与用户旅程.md** | 五块 MVP 功能的产品 spec（用户故事 / 验收 / ICE 优先级）+ 产研运负责人四角色端到端旅程 | 知道 MVP 做哪五块、谁怎么用、先做哪个 |
| 4 | **05-ezagent技术开发计划.md** | 技术架构（四柱一地基）+ 要链接的 plugin/agent/external_mirror 清单（现成/要写/在建）+ 四阶段开发计划（每阶段一条 e2e 验收）+ 风险表 | 知道技术怎么落、分几阶段、~9.5 周排期 |
| 5 | **02-战略到ROI闭环与案例.md** | 把静态大图变成会转的发动机：战略→导图→每周选题→每日开发→dogfooding→运营→ROI→反哺的端到端闭环 + 周历 + 6 家公司案例 | 知道这套东西怎么"转起来"、谁这么干过 |
| 6 | **06-产品闭环与有效性评估.md** | 收成可照着干的七步 SOP + 五个关键机制的有效性体检 + 必要性评估（MVP 必须 vs 后置）+ 三种失败模式 | 知道怎么执行、哪些机制靠谱、怎么防它退化 |

**速记**：03 给骨架 → 01 选工具 → 04 写 spec → 05 排技术 → 02 让它转 → 06 体检它有效。03 是逻辑链权威，其余五篇都引它。

---

## 当前确定性结论（已对齐，可据此开工）

1. **真相源永远在 ezagent 自己的库（默认 SQLite）**，外部工具都只是"能被 agent 自动读写的产物容器 + 协作面"，靠稳定 ID/URL/路径当外键挂回节点（`01-工具选型.md:17`）。
2. **进出只有两条铁路**：出站 external_mirror（`:push`/`:pull`）、入站统一 `Ezagent.Invocation.dispatch/1`（P14 铁律，不许 PubSub.broadcast 到入站 topic）。
3. **MVP 五块功能 + ICE 排序已定**：① 节点认领（P0 地基）② 产物挂载（P0，依赖①）③ 个人 agent 追踪（P1）④ 网页对齐出口（P1）⑤ 闭环看板（P2）（`04-spec与用户旅程.md:153`）。
4. **底座够用、只缺一个核心数据模型**：会话/agent/出口/权限/审计/飞书出入站都现成；唯独"思维导图 + 节点 + 认领 + 挂载"必须自研（最大块），GitHub 双向同步是第二大块。
5. **技术架构定型**：四柱一地基——地基=自研 mindmap domain；柱1=socialware public_view 出口；柱2=Entity.Agent+curl flavor；柱3=external_mirror 出站+入站 plugin；柱4=SQLite 真相源（`05-ezagent技术开发计划.md`）。
6. **MVP 边界明确不做**：world / agent-schema（在建未进 main，别压排期）、精细分租户、复杂结算、不自研导图编辑器（先 xmind 承载 + 关键结构镜进 workspace）。
7. **MVP 必须 vs 后置已分**：必须=节点认领+产物挂载(GitHub/飞书)+日spec/周闭环节奏+一条北极星+固定模板出口；后置=cc 重 flavor、动态编排器 UI、闭环看板、xmind/excalidraw/obsidian connector、PMF 引擎（`06-产品闭环与有效性评估.md` 第三部分）。
8. **北极星 + 哨兵指标已定**：北极星=周用户价值闭环数；四哨兵=周闭环数 + 对齐时长下降 + 挂载覆盖率 + cycle time，用来早警三种失败模式。
9. **方法论不是新发明**：Amazon PR/FAQ + Teresa Torres 机会-方案树 + RICE/ICE + Shape Up + Linear cycle/changelog + Superhuman PMF + Lean BML，七套验证过的方法缝成一张活图。

---

## 待验证 / 待决策项（开工前或开工中要解掉）

> 这些是文档里诚实标注 unverified、或机制评估里点名的高风险点。**不要当成已确定能力排期。**

**技术 unverified（`05` 风险表 R1–R10）**：
- **R1 页面树 schema 能力边界**——json-render 能不能渲染"导图/看板"这种复杂结构？撑不起就降级列表/表格视图（阶段 1 先实测）。
- **R4 节点建模成 Kind vs 普通记录**未定——影响并发/生命周期/LOC 预算，阶段 2 开工前要出一个 mini design。
- **R7 飞书文档变更事件能否订阅进 dispatch**未实测——不行就降级 agent 定期拉取。
- **R8 CapBAC 5 轴用于"节点认领"的语义匹配度**未验证——不够就在节点 slice 补认领元数据。
- **R9 curl flavor agent 能否被触发式"主动盯"**（节点变化→主动 notify）未实测——不行就 cron/定时 dispatch 唤醒。

**机制层最高不确定性（`06` 有效性评估）**：
- **个人 agent 是案例支撑最弱的差异化赌注**——没有现成公司做过完全一样的事。最大失效风险是 **agent 噪音**（N 个 agent 刷屏，对齐成本不降反升）和 **答不准**（和真相源漂移）。决策：MVP 先用最轻 curl flavor 验证"能追踪+能对话"且对齐时长真降，再决定是否放大/上 cc。
- **网页出口落地坑最多 + 依赖在建**——四坑（活会话同 BEAM / 字面布尔 true / SPA 必须 build / 编排器偏编排非画 UI）+ world 未进 main。决策：MVP 用固定模板视图兜底，动态 UI 后置。

**已知但仍需排期决策**：
- R5 GitHub Projects v2 必须走 GraphQL（REST 不够）——MVP 可先只做 issue/PR（REST 够），Projects v2 列后续。
- R3 LiveView 管理面有确定性测试失败 + 部分按钮坏——绕开用 HTTP API / iex RPC 建会话。

---

## 下一步行动

1. **按 `05` 阶段 0 起脚手架**：建 mindmap domain 空壳（`apps/ezagent_domain_mindmap/`）、一次性 build 客户端 SPA、初始化隔离 home + 库。验收=`mix test` 全绿 + `/socialware/chat` 返 200 不空白。
2. **跑阶段 1（里程碑 1，~1.5 周）出"能看的东西"**：用现成能力组合——public_view 会话模板 + 一个 curl flavor 个人 agent + 编排器吐静态占位树。先验证 R1（页面树能投什么）。
3. **阶段 2 开工前出 mindmap mini design**：先解 R4（节点 Kind vs 记录）、R8（认领用 Grant 是否够），再进最大块自研（节点状态机 + 认领 + 飞书挂载，~3 周）。
4. **阶段 3 GitHub 双向同步**：先做 issue/PR（REST），Projects v2（GraphQL）按需后续；同期实测 R7（飞书变更事件）。
5. **第一圈 dogfooding 主循环先精简**：3–5 人团队第一圈只跑"日 spec + 周 changelog"，PMF 测分/访谈轮值/下注会后置，防过度仪式化。
6. **从第一天就埋四个哨兵指标**（周闭环数 / 对齐时长 / 挂载覆盖率 / cycle time），每周日 S7 复盘看它们动没动——这是判断设计有没有真的有效的唯一标准。

---

## 一句话收口

这套文档已经把"做什么（03 骨架/04 spec）、用什么（01 工具）、怎么落（05 技术）、怎么转（02 闭环）、靠不靠谱（06 评估）"五件事讲透，确定性结论足够开工；**真正的不确定性集中在两块——个人 agent 这个差异化赌注（案例支撑最弱，先用最轻 curl flavor 验证）和网页动态 UI（依赖在建 world，先固定模板兜底）**。下一步就是按 05 的四阶段往下走，从阶段 0 脚手架 + 阶段 1 出"能看的东西"开始，边做边解 R1/R4/R7/R8/R9，让发动机先转起来。

---

## 俞军评审与整改

> 评审报告：`_yj-review.md`（俞军方法论 evaluate 套件，A 类内部 spec 打底 + C 类"机会成本/可持续"加重 + B 类"竞品核验"）。整改对照表：`_revision-log.md`。

**一句话评审结论**：作为**对外产品方案不合格**、作为**内部 dogfooding 工具 + 一次 ezagent 技术验证有条件合格**——技术落地分析扎实、证伪意识和诚实度突出，但产品层有三处硬伤：用户情境混淆导致对外用户价值净值为负、最致命的 agent 赌注没有价值验证、回避了"是不是一门生意"。

**整改了哪些关键点**（已落进 00–06）：
1. **定位收口为内部 dogfooding 工具**（02 §〇、03、04 §〇、01 §0.5）——情境 A（自用）/情境 B（对外卖）分层核算，对外的 PMF 测分/PLG 自助/差异化叙事一律降级为"待情境 A 验证后再议"，不脚踏两只船。
2. **agent 核心赌注的价值验证前置到阶段 1**（05 阶段 1 价值闸门、04 功能 3、06 机制 3）——用最轻 curl flavor、3–5 人、2 周实测"对齐时长真降了没"，降了才往下、没降就停，把 9.5 周风险压到 2 周。
3. **真算一次内部 ROI**（06 第二点五、02 §〇之二）——给出回本公式 + 机会成本（这 9.5 周不做 ezagent 主线的什么）必须显式写出。
4. **北极星换成有外部检验的结果指标**（02 §〇之二、06 S1/S7、03 §8.1）——主结果=「对齐时长下降」，「周闭环数」降级为活动指标。
5. **竞品分析替代案例背书**（06 第二部分、02 第四部分）——Linear/Notion/Figma 等改写为竞品逐条对照"它做到什么程度/我们凭什么不同"，参照系从背书换回竞争。
6. **补用户非货币支付（价值卡）+ 团队画像分档 + 仪式最小集收口**（04 每功能价值卡、03 §5、02 §3 周历：MVP 第一圈只强制"日 spec + 周 changelog"）。

**仍待人工决策的开放问题**：
- **二选一拍板**：长期就停在内部工具，还是择期转对外产品？若转对外，需另补 ICP 收窄（旧体验≈0 的新组队团队）+ 迁移路径 + 定价三件。
- **ROI 实测数填值**：回本公式里的"每周省时 X"必须由阶段 1 实测填入；机会成本如何相对 ezagent 主线定价，需团队拍。
- **核心赌注闸门红了怎么办**：阶段 1 实测对齐时长未降时的止损/转向决策，文档只给"停"，未给替代路径。
- **06 内部遗留口径**：06 §1.0/S6/S7 的速查 SOP 仍保留旧 PMF/周闭环表述（已在 S1/§二点五/小结处标注修正口径），是否回头统一改写待定。
