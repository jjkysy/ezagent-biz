# ezagent / esr-ng —— 25 app 详细汇总

> **基线：`upstream/main` @ `5d540f535`（2026-07-06 全量 re-bootstrap）。**
> 本文件由 12 组 bootstrap module-report（25 app 全量现读）汇总，每条断言尽量带 `file:line`（从 report 取，未二次编造）。
> 少数 app（agent_bridge / external_mirror / python / pty / curl / kb / native / email / protocol_api / feishu / cli / ui）自 de4f40a5 基线以来生产 lib 字节未动，只有 test/行号刷新；file:line 已对齐 HEAD。
> 测试状态一律 best-effort，本轮 bootstrap **未现跑**（需 mise OTP27/1.18 隔离工具链 + Postgres）。
>
> 三层（P9 "读什么数据决定归属"）：**core（L1 机制）→ domain（L2 领域）→ plugin（L3 扩展）**；`ezagent_cli` / `ezagent_web` 是 transport 顶层（P13）。
> 已裁决冲突：`Ezagent.Session.Membership.authorize` 现役是 **3 参**（`authorize(slice, caller, session_uri)`，membership_predicate.ex:55），`/2` 仍存但只 delegate 到 `/3` 传 nil（:42）；#1175 后 socialware 三处读全传 session_uri（external_feed.ex:402 / chat_feed.ex:137 / gc.ex:212）。moduledoc 里 "authorize/2" 是 stale。

---

## L1 core

### ezagent_core（L1 core，tier-agnostic 机制库；零 umbrella dep）

- **entry**：`apps/ezagent_core/lib/ezagent_core/application.ex:9`（`EzagentCore.Application.start/2` 监督树）。dispatch 入口 `lib/ezagent/router.ex:79`（`Router.dispatch/1`）+ legacy `lib/ezagent/invocation.ex:118`（`Invocation.dispatch/1`）。
- **拥有**：dispatch pipeline、Kind actor runtime（GenServer + snapshot/event-log + 可靠性原语）、ActionSet 契约 + effect 语法、CapBAC、URI SPEC v3（6 scheme）、Plugin 授权契约 + 两阶段声明式 boot、PluginPackage manifest、flavor-agnostic Recipe、routing registry/matcher、audit。

**Dispatch 三原语（Router / ActionSet / Kind）**
- `Ezagent.Router` — dispatch 原语，`%Cmd{}` → 13 步 middleware pipeline；Phase 1 策略翻译成 `%Invocation{}` 复用 legacy 路径（router.ex:79 dispatch/1；:96-97 to_invocation |> Invocation.dispatch；:121 dispatch_saga/2 loaded-guard 返回 `:saga_runner_not_loaded` 非崩溃）。`safe_call/3`（:276）对 EventLog/SagaRunner 未加载静默跳过。
- `Ezagent.Invocation` — universal request 形状 + 12 步 dispatch 契约（invocation.ex:1）；dispatch/1（:118）先 `WorkspaceOwnerGate.assert_local_owner_for_uri` + idempotency check 再 `dispatch_with_lazy_spawn`；`:subscribe`/`:introspect` 返回 `:unsupported_mode`（:114）。cold-activation budget 默认 20_000ms（`@default_activate_budget_ms` :48，可 `config :ezagent_core, :activate_budget_ms` 覆盖 :57；bump 自 5s 因 cc heavy flavor 冷启动 >5s）。step 5.6 workspace isolation 返回 `:cross_workspace_denied`（moduledoc :16-20）。
- `Ezagent.ActionSet` — 契约模块（**T1 rename**：文件仍 `lib/ezagent/behavior.ex:1`，模块名 `Ezagent.ActionSet`）；`use ...` + `action` 宏 + `handle_<action>/2` → `{:ok, result, [effect]}`。9 effect（`:set`/`:emit`/`:dispatch`/`:notify`/`:effect`/`:effect_returning`/`:saga`/`:terminate`/`:halt`）。子模块 `Ezagent.ActionSet.{Manage,Routing,Notifications,Introspection,Terminable,KindBase,Sandbox}`（`lib/ezagent/behavior/*.ex`）。effect 语法在 `behavior/effects.ex`。**开发者唯一 surface 是 `use Ezagent.Lifecycle`**（不写 `invoke/4`，`@optional_callback` only）。**例外**：`Ezagent.BehaviorRegistry` 模块名**没**改（`behavior_registry.ex:1`，CLI/ApiV1 仍消费它——正确非 bug）。
- `Ezagent.Kind` — Kind 声明宏（kind.ex，901 LOC）。runtime 在 `kind/runtime.ex`（992，dispatch 步 5-10 在 Kind GenServer 内）；GenServer host `kind/server.ex`（1000）；snapshot/state-rebuild `kind/snapshot.ex`（807）+ `kind/state_rebuilder.ex`。
- `Ezagent.Entity.System`（entity/system.ex:1）— core 唯一 Kind；boot spawn `system://routing/default`（`:test` 跳避 SQL.Sandbox race，application.ex:243-278）。

**可靠性原语（P22，全在 core，boot 于 application.ex）**
- ReadyGate（ready_gate.ex）/ PendingDelivery（pending_delivery.ex）/ Idempotency + Sweeper（idempotency.ex + idempotency/sweeper.ex，boot application.ex:29）/ DLQ（dlq.ex）/ Audit.Writer（audit/writer.ex，application.ex:67，**`:test` 跳**避 SQLite busy）/ Snapshot.Writer（snapshot/writer.ex，application.ex:88，**`:test` 跳**）。skip 逻辑 `skip_in_test_env?/1` + `@writers_skipped_in_test`（application.ex:201-204），invariant test 双向 pin。
- **#1193 reputation Receipt（新）**：`Ezagent.Kind.Runtime.Receipt`（kind/runtime/receipt.ex，165 LOC）——dispatch chokepoint minimal facts 层。仅 **cross-workspace** success 路径 emit 一行 `:receipt` EventLog（maybe_emit/5 :54，payload=caller/provider/capability identity_key/action/trace_id/outcome + **reserved-but-empty** signature/provenance_ref :71-73）。`cross_workspace_invocation?/3`（:96）复用 dispatch step 5.6（`ActionSet.workspace_scoped?` + `Capability.workspace_of`）。exhaust 语义：失败全吞不影响 dispatch reply（belt+suspenders :84-92）。runtime/effects.ex +19（emit_receipt/3）。
- **#161-B cascade hook（新）**：`Ezagent.Kind.CascadeHook`（kind/cascade_hook.ex，68）——slice-mutation emit chokepoint。`Kind.Server.commit_and_notify/3` 在 `SliceChange.emit/1` 后调 `maybe_enqueue/1`（:41）；allowlist `{"entity", :identity}`（:32，cap grant/revoke 变 identity 切片）触发 self-targeted fire-and-forget `:cascade_notify_managers` dispatch，走 `DeferredDispatch`（:61，post-commit turn，离 mutating critical path）。ctx `caller: :vm_internal`（:58，不可伪造）；content-free payload。

**CapBAC**
- `Ezagent.Capability`（capability.ex，590）：`defstruct kind/behavior/action/instance/workspace_uri`（:40，workspace_uri 默认 `:any` 非 enforce）；`identity_key/1`（:523，5-tuple）、`workspace_of/1`（:425）、`cross_workspace?/1,/2`（:340/:357）。子模块 `capability/{match,normalize,parser,scope,unauthorized}.ex`。`CapabilityRegistry`（capability_registry.ex，511）+ subjects/defaults。

**URI SPEC v3（6 scheme）**
- `Ezagent.URI`（uri.ex，989）：6 scheme `entity/workspace/session/template/resource/system`（moduledoc :129）；`@unified_per_tenant_schemes ~w(entity session template resource)`（:515，3-segment authority）；`per_tenant/4`（:488）。`uri/scheme_registry.ex` + `uri_query.ex` + `uri_query/scan.ex`（888）。boot seed `seed_uri_schemes/0`（application.ex:211-219，6 scheme 明列 :214）。

**Plugin 契约 + 热装**
- `Ezagent.Plugin`（plugin.ex，965）：**无 `definitions/0`**。callback = `plugin_info/0`（必需 :195）+ 可选 `kinds/behaviors/spawns/template_classes/agent_flavors/roles/adapters/routing_tables/resource_types/config_surface/children/after_boot`（`@callback` :195-244，`@optional_callbacks` :246，default no-op :282-293）。boot `publish/1`（:441）：先 `reject_spawns!`（:445/:536，`spawns/0` RESERVED 必 `[]`，非空 hard-raise 防插件劫持 core scheme dispatcher）→ register behaviors/agent_flavors/template_classes → `SurfaceValidator.validate_surfaces!` → `RoleSeedHook.seed_roles`（:482，recipe SEED 源，domain-agent 注册前 no-op seam）→ routing_tables → `publish_adapters!`（:501/:798，all-or-nothing 带 rollback）。
- **ExternalMirror adapter axis = push+pull**（P3-1）：push = `{adapter,binding}` 元组（两 registry 都写），pull = 裸 adapter module（仅 AdapterRegistry，pull 无 per-binding transport，tuple-wrap 报错 :637/:705）；socialware customer feed 是首个真 pull adapter（moduledoc :149-152）。
- `Ezagent.PluginPackage.Manifest`（plugin_package/manifest.ex，383）— 声明式 package manifest。**仍拒 `:socialware` seed_ref**：`seed_ref` type `kind: :recipe`（:59-60）；`validate!/1`（:140）对非 `:recipe` kind raise ArgumentError（:175-178，"socialware-def seeding is a future enhancement — rejected, not a no-op"）；`parse_seed_refs/1`（:354）同拒（raise :360-363）。moduledoc :51-58 标注 rejected-not-silently-dropped。
- `PluginPackage`（plugin_package.ex，297）+ installed_registry；`plugin_asset_registry.ex`；热装 mix task `mix ezagent.plugin.install`（mix/tasks/ezagent.plugin.install.ex，283）+ `plugin.unload`。

**Recipe 原语（flavor-agnostic）**
- `Ezagent.Agent.Recipe`（agent/recipe.ex，292）：构造性 flavor-agnostic，`new/1`（:87）拒 `@flavor_fields [:flavor,:kind,:bridge_adapter,:template_class]`（:34，atom+string 双形 `flavor_key_present?` :92，返回 `{:error, {:flavor_field_in_role, key}}`）+ shape-validate。字段 `name/passive/skills/plugins/prompt/script/behaviors/requested_caps/session_template/config`（defstruct :47-56）。requested_caps 是 *request* 非 grant（moduledoc §2.3.1 :25），materialize 时 fail-closed 授权。`@cap_materialization_axes` 禁 recipe cap 携带 kind/instance/workspace_uri/granted_by/granted_at（:41，CapBAC 洞防护）。子模块 `agent/recipe/{compose,cap_mint}.ex` + `agent/materializer.ex`（579）。

**Routing / arch gate / 其它**
- `routing_registry.ex`（289）+ `routing/{matcher,resolver,rule_store,legend,receiver,prompt_template}.ex`（resolver 617、rule_store 387、matcher 316）。RuleStore boot 水化 durable rule 进 live registry（G1-b）。
- `mix ezagent.arch.scan`（mix/tasks/ezagent.arch.scan.ex，1315）— 命名 counter + cap（fitness functions）；`measure/0` :302（`:persistent_term` 缓存），`do_measure/0` :314 列 counters（oversized_modules / def_count_* / spawn_registry_call_sites / cross_file_duplicate_fn_groups / cc_bridge_shim_callers / `:set` effect grep 等）。arch.scan 冻结锚随 #1193 从 169→157。
- `mix ezagent.check_invariants`（mix/tasks/ezagent.check_invariants.ex，438）— 8 硬不变式 grep gate。其余 mix task：`ezagent.doc.scan`（628）、`caps.audit`、`uri_query.scan`、`stress`（613）、`bootstrap`、home init/backup/restore/adopt_db、snapshot dump/list/clear、e2e.run、db.reset、world.migrate_layouts、session.migrate_slice、kind_base.backfill、credential.adopt。
- 编译期 gate `Mix.Tasks.Compile.EzagentPluginCheck`（mix/tasks/compile/ezagent_plugin_check.ex，895）。
- Credential 子系统（credential/*.ex：resolver 409、home_runtime 337、grant_row 222、cascade_runtime、user_default_source、workspace_shared_source、grant_cap、adopt）。EventLog（329）/ EventSubscriber / SnapshotStore（339）/ SagaRunner（328）/ MessageStore（456）/ SliceChange（300）/ NotificationSubscriptions（`:protected` ETS GenServer，567，boot application.ex:78）/ Notifications / Presence + Tracker / Audit（451）。Runtime（OS process：runtime.ex 217 + runtime/{os_process,pid_file,line_buffer,orphan_reaper}.ex；erlexec dep）。Home（home.ex + home/migration.ex 604）。

**对外契约 / 易混淆点**
- **P14 dispatch-only**：inbound 唯一路径 `Router.dispatch/1` / legacy `Invocation.dispatch/1`；不许 PubSub.broadcast 到 inbound topic。
- **`Ezagent.Behavior` 文件名 vs `Ezagent.ActionSet` 模块名**：T1 后契约模块叫 ActionSet，文件仍 behavior.ex、子目录 behavior/。旧 `invoke/4` 是 `@optional_callbacks` 无 runtime 路径。
- **role vs recipe 消歧（Gate B）**：core `Sandbox` ActionSet durable slice 字段从 `:role` 改名 `:recipe`（#1185，build provenance，非 session role；session role_name 只住成员边）。
- **层纯度**：mix.exs:44-57 只声明 Hex deps（dns_cluster/phoenix_pubsub/phoenix/postgrex/jason/yaml_elixir/erlexec），零 umbrella dep。**DRIFT**：application.ex 多处注释写 "SQLite"（child ④ :41、skip_in_test_env? :186-196），真 dep 是 `:postgrex`（Postgres）——注释 stale 非 bug。
- `config_surface/0` 留 core callback，但 `nav_surfaces/0`+`session_tabs/0` 已搬 World（`Ezagent.World.UiSurfaceProvider`，duck-typed，plugin.ex:456-458）。

**自 de4f40a5 的变化（4 个改 core lib/）**：#1193 Receipt（新 kind/runtime/receipt.ex）；#161-B CascadeHook（新 kind/cascade_hook.ex）；#1185 Sandbox `:role`→`:recipe` + 新 Gate B arch test `agent_session_role_gate_test.exs`；#161-A1 arch.scan spawn_fresh 锚调整。#161-A2/C、registry P0、#160、hello orchestrator、socialware role-slot P1 对 core lib 只碰 arch/invariant baseline。

**测试**：`test/` 下 **252 个 `*_test.exs`**（best-effort）：architecture/（25 arch gate）、invariants/（~90）、e2e/（scenario 14/15/17/24/25/30/34）、integration/、ezagent/（含新 kind/runtime_receipt_test.exs）、mix/ + 6 个 fixtures/ezagent_plugin_*（含 em_pull/em_dual/em_mismatch）。

---

## L2 domain

### ezagent_domain_agent（Tier-2 Domain LEAF）

- **entry**：`apps/ezagent_domain_agent/lib/ezagent_domain_agent/application.ex:25`。**tier/deps**：im→session→agent 的 LEAF；mix.exs:44,47,48 只 dep core + identity + agent_bridge，**从不 dep session/im 或 plugin**。
- **拥有**：统一 Agent Kind + AgentTemplate Kind、`agent.receive`→AgentBridge 交付缝、flavor 数据簇（registry/resolver/attributes）、role=recipe 读模型（RecipeRegistry read-through over ConfigStore）+ recipe×flavor 物化（RecipeMaterializer/DefaultAgentSeed）+ 两个 reparented flavor Behavior（CcHeadlessAgent/CurlAgent）。

- `Ezagent.Entity.Agent`（agent.ex:1）— Agent Kind（`type_name :agent` :55）。persistence `{:snapshot,:on_change}`（:225，从 `:on_terminate` 提升让 CLI cap grant durable）。`behaviors/0` = base（:141-159：Identity/Sandbox/ApiKeys/CredentialGrant/ConfigEvolve/ConfigGovernance）∪ 每个注册 flavor 的 `instance_behaviors` thunk（:161-170）。flavor 子集辅助 `curl_behaviors`（:181）/ `cc_headless_behaviors`（:191）/ `nil_capture_behavior_set`（:200）。`spawn_fresh/4` TOCTOU-硬化 `:already_started` 零副作用（:341-367）；`session_instance_name/3`（:433-449）；`list_in_workspace/1` 从 kind_snapshots 扫 live+dormant（:74-91，#161 member-cap 用）。多个 defdelegate → `TemplateSpawn`（:480-497）。
- `Ezagent.Entity.AgentTemplate`（agent_template.ex:1）— Template Kind（`type_name :agent_template` :163），behaviors `[Identity, Template]`（:170），persistence `{:snapshot,:on_change}`（:173）。`to_template_data/2`（:287）= 通用 base（class/agent_uri/cwd/config_dir/cascade）+ flavor `template_data_extra/1`；`config_dir` 是 UNIVERSAL 中性 key（:349）。`resolve_template_class/1` 经 AgentFlavorRegistry（:528）。
- `Ezagent.ActionSet.Agent.Receive`（receive.ex:1）— `agent.receive` `:cast`（:92），`use Ezagent.Lifecycle, state_slice: :session`（:79），`reads_siblings([:sandbox,:identity])`（:84）。**member-cap 授权在处理器 TOP**（`Session.MemberReceive.authorize(ctx)` :207，#161-A2），`:receive` cap-exempt（:283）。self-message loop guard（:213-223）。按 `Delivery` CLASS TAG 分支：`:ok`（subprocess_ws 异步）/ `{:sync,flavor,result}`（in_process_sync → 再 dispatch `:sync_result` effect，:228-246）。`sync_result_action/1` 按 flavor 选 action：cc-headless/py/**hello**/curl-default（:340-346）。
- `Ezagent.ActionSet.Agent.Delivery`（delivery.ex:1）— agent 域交付缝，`deliver_agent_receive/2` 建 flavor-neutral `AgentBridge.Payload` 交付，返 CLASS TAG（:36-132）。flavor 解析三层：ctx-sandbox → ETS `AgentFlavorAttributes` → durable snapshot（:137-177，deadlock-safe 不做 `Kind.get_slice` self-call）。`in_process_sync?` 查 `AdapterRegistry.transport_class`（:182）。
- `Ezagent.Agent.RecipeRegistry`（recipe_registry.ex:1）— role=recipe 读模型，read-through over `Socialware.ConfigStore`。subject `recipe:<name>`（结构化非-URI :92-95），key `"recipe"`（:57），layer `"workspace"`（:61）。`lookup/2` 解析序：ETS cache→caller-ws ConfigStore→system-ws fallback→`Recipe.new/1` rehydrate→cache（:117-147）；ETS 是 invalidate-on-publish cache 非权威。cap VALUE 在 rehydrate 边界 canonicalize（:160-193）。`seed_role_if_absent/2` atomic（:256-276）。`retire_role/1`（:290-320）。
- `Ezagent.Agent.RecipeMaterializer`（materializer.ex:1）— recipe×flavor 物化。`template_content/2`（:45）；`create_agent_from_recipe/1` 经 `Agent.spawn_from_template_content/5`（:85-117）。`record_launch_attributes` 只写 BUILD PROVENANCE（recipe name）到 `AgentRecipeAttributes` + `AgentPassiveAttributes`，**绝不写 session role_name**（:204-211，Gate B）。
- `Ezagent.AgentFlavorRegistry`（flavor_registry.ex:1）— 声明式 `flavor → %{kind, template_class, instance_behaviors, cap_policy}` ETS map。`register/1` 幂等冲突 raise（:68-98）。seal 机制 `seal!`/`sealed?`/`await_sealed`/`note_plugin_published`（:124-155），全 flavor plugin publish 后触发 `Workspace.Loader.load_all`（:213-253）。
- `Ezagent.AgentFlavorResolver`（resolver.ex:1）— deadlock-safe flavor 解析（session 域 `:flavor` resolver + agent 域 delivery 共用）。`flavor_from_durable_snapshot/1` 扫 snapshot：先持久 `:flavor` slice 字段（curl），**再 `:sandbox` slice template_class/respawn（py，#1203 修）**（:30-103）。只读 ETS+SnapshotStore。
- `Ezagent.AgentRecipeAttributes`（recipe_attributes.ex:44）— per-URI recipe(build-provenance) ETS `:ezagent_agent_recipe_attributes`。`put/2`/`fetch/1`（ABSENT→`:none`）/`delete/1`。durable 真相在 `:sandbox` slice `:recipe`（:20-27）。**P2(#1185) 从 `AgentRoleAttributes` 改名，"role"与"recipe name"消歧，记 BUILD PROVENANCE 非 session role**（:5-11）。
- `Ezagent.AgentRecipeResolver`（recipe_resolver.ex:1）— recipe-provenance LIST 读模型。`list_by_recipe/2` 从 kind_snapshots 扫 live+dormant（:58-77，cold-restart-safe，K4 workspace 作用域）；`recipe_from_durable_snapshot/1`（:89），读 `:sandbox.:recipe`（:131-142）。
- `Ezagent.AgentFlavorAttributes`（flavor_attributes.ex:12）/ `Ezagent.AgentPassiveAttributes`（passive_attributes.ex:38，RF-6 non-principal，fail-closed-to-PRINCIPAL）— flavor/passive ETS 快径。
- `Ezagent.Agent.Config`（config.ex:1）— console-facing config cascade 门面，manage-cap gated（经 `Identity.caps_authorize?`，读不 dispatch 避 activate_timeout :42-49）。default key `"agent.soul"`。
- `Ezagent.Agent.CredentialStatus`（credential_status.ex:1，**#160 新**）— flavor-agnostic 登录状态 + probe router（read-only 不激活）。enum authenticated/expiring/expired/missing/n_a/unknown（:43）。按 TC credential 契约路由：file-credentialled（cc/codex `credential_status/2`）/ slice-credentialled（curl `:api_keys` key count）/ neither→`:n_a`（:86-98）。
- `Ezagent.Entity.Agent.TemplateSpawn`（template_spawn.ex:1）— content-taking spawn 助手。承担 post-spawn 义务（lineage+workspace bind，只 `fresh?: true` :428-459）+ 失败 rollback。**#1202 改 FIRE-AND-FORGET `:cast`**（去掉 `ReadyGate.await`，首次 socialware install 5s 超时修复，:641-691）。`TemplateSpawn.Cascade`（cascade.ex）credential cascade 解析（`resolve_content/7` 3 分支）。
- `Ezagent.ActionSet.CcHeadlessAgent`（cc_headless_agent.ex:10）/ `Ezagent.ActionSet.CurlAgent`（curl_agent.ex:1）— 两个 reparented flavor STATE 半（`use Ezagent.Lifecycle`）。curl `:curl_agent` slice，actions `:reset_conversation`/`:configure`/`:sync_result`，cap axis `:agent`；`handle_sync_result/2` 持久对话+token+error+`session.send`（adapter 只 transport 不持久）。
- `Mix.Tasks.Ezagent.Agent.GrantRecipeCaps`（grant_recipe_caps.ex:1）— p7 sanctioned grant 入口，fail-closed no-partial（每 cap behavior 先解析到 LOADED module :143-166），least-priv admin-scoped；`instance_overrides` 支持跨-instance board-scoping（pm-coordinator→board）。
- `Ezagent.Agent.DefaultAgentSeed`（default_agent_seed.ex:1）— 通用 template-seed 引擎（`cc × <role>`），flavor `"cc"`（:63）。boot 只 WRITE template，grant 移到 GrantRecipeCaps mix 任务（p7）。
- hooks（declare-don't-call）：`RoleSeedHook`（seed recipe，`:test` 跳 DB write）、`PackageSeedHook`、`FlavorPublishHook`、`FlavorTemplateHook`。transport readiness：`TransportReadiness`（ETS 外部门，#505）+ `TransportReadinessListener` + `LiveJoinRegistry`。`EtsOwner` 拥 5 张 ETS 表；`EzagentDomainAgent.Application` boot 后注册 **7 个 cross-domain seam**（Invariant #8，application.ex:51-64）。

**易混淆点**：flavor 是 DATA 非 code（本 app 无 `agent_flavors/0`/`roles/0`，是 MECHANISM）；role=flavor-agnostic recipe（`Recipe`/`CapMint`/`Compose` 在 ezagent_core 不在此）；`AgentRecipeAttributes`(build-provenance) ≠ session role_name（后者只活成员边，Gate B）；`agent.receive` flavor-BLIND（按 CLASS TAG 非 flavor 名）；frozen supervisor 名 `EzagentDomainInstanceMessage.AgentSupervisor`（从 session 域搬来未改名，D1a）。

**自 de4f40a5（7 commit）**：#1203（py sandbox 回退修 "@mention py agent → no reply"）、#1202（TemplateSpawn fire-and-forget cast 修首次 install 5s 超时）、#1185（de-bake role：删 session_agent_materialize.ex -288、`agent_role_*`→`agent_recipe_*` 改名）、#1171（新 CredentialStatus）、#161-A2（receive TOP member-cap authorize）、#161-A1（list_in_workspace）、#1168（sync_result_action("hello")）。

**测试**：13 个测试文件（config_api/config_crud/config_no_activation/credential_status/flavor/grant_recipe_caps_board_scope/recipe_registry/transport_readiness + agent_flavor_ets_owner/agent_flavor_resolver_durable_sandbox + entity/agent_*）。

### ezagent_domain_agent_bridge（Tier-2 Domain；声明 NO Kind/ActionSet/recipe/flavor）

- **entry**：`apps/ezagent_domain_agent_bridge/lib/ezagent/agent_bridge.ex:11`（`Ezagent.AgentBridge.deliver/2` 门面）。deps 只 core + phoenix + yaml_elixir，不 dep plugin。**自 de4f40a5 无 commit**。
- **拥有**：flavor→adapter 解析 + 两 transport class（`:subprocess_ws` vs `:in_process_sync`）+ `agent_uri→channel_pid` ETS registry + per-agent connect-token store + Phoenix Socket/Channel + self-healing delivery。
- `Ezagent.AgentBridge`（agent_bridge.ex:1）— `deliver/2` 按 `resolve_flavor`（:326）+ `AdapterRegistry.transport_class` 分支：`:in_process_sync` → `deliver_in_process`（绕 Channel/Registry/readiness，`adapter.deliver(payload, nil)` 同步 :288-296）；`:subprocess_ws` → channel 查找 + `deliver_or_buffer`。`deliver_ensuring/3` 自愈消失 bridge（subscribe-then-recheck + heal_fn relaunch + await rebind，重试一次 :91-207）。`default_heal/1` 从 SnapshotStore 读 sandbox（deadlock-safe DB 读 :237-276）。
- `Ezagent.AgentBridge.AdapterRegistry`（adapter_registry.ex:1）— GenServer，flavor→adapter 注册。`register/2` 校验 loaded/`@behaviour`/flavor 匹配/transport_class ∈ `[:subprocess_ws,:in_process_sync]`（:118-150）。`transport_class/1` 缺省 `:subprocess_ws`（:44-50）。`deliver_or_buffer/3`（adapter 未注册 buffer，TTL 5s + per-flavor 100，drain-on-register :157-224）。**≠ `Ezagent.ExternalMirror.AdapterRegistry`（adapter_id-keyed，别 app）**。
- `Ezagent.AgentBridge.Registry`（registry.ex:11）— 统一 `agent_uri→channel_pid` ETS `:ezagent_plugin_agent_bridges`。`bind/3` 幂等（:41-66）；`unbind/2` **PID-GUARDED**（只删自己 row，防 cc reconnect churn 误删 :94-109）；`unbind/1` force。broadcast 到 `esr:agent_bridge:bridges` + legacy `esr:cc_channel:bridges`。
- `Ezagent.AgentBridge.Adapter`（adapter.ex:1）— transport-neutral per-flavor behaviour。callbacks flavor/transport_class/deliver（channel_ref = pid|nil）；WS-only optional handle_client_event/join_info/socket_path/channel_topic_prefix。`deliver/2` 返 `:ok | {:ok,term} | {:error,term}`（勿窄化，curl 依赖 `{:ok,result}`）。
- `Ezagent.AgentBridge.Channel`（channel.ex:1）— Phoenix.Channel host 一 bridge sidecar。topics `agent_bridge:<flavor>:<uri>` + legacy `cc:bridge:<uri>`。join 校验 topic-URI + flavor + `verify_transport_class`（只 `:subprocess_ws` 可 WS join）。`Socket`（socket.ex:1）`/agent_bridge` 入口，token via TokenStore；`TokenStore`（token_store.ex:1）per-agent connect token（YAML `system://credentials/cc-channels.yaml`，常时比较）；`Payload` flavor-neutral 信封；`AttachmentNormalizer` 归一。

### ezagent_domain_external_mirror（domain tier；invariant 15 — 拥有每一条 OUTBOUND mirror）

- **entry**：`lib/ezagent_domain_external_mirror/application.ex:76`。boot：WorkerRegistry→RootSupervisor→FacadeNonceTable→TargetCheckTaskSup→SubscribeTaskSup→BootReconciler（application.ex:81-119）；`register_worker_behavior/0`（:149）注册 Worker `:publish` cap subject。**自 de4f40a5 零 commit（字节相同）**。
- **拥有**：Publisher 契约 + Session 上 bind/unbind/list Behavior + forgery-proof cap-gated facade + per-binding Worker Kind + AdapterRegistry/BindingRegistry + adapter KIND 轴（push/pull/request_scoped）。deps 只 core + identity（**刻意不 dep session**，读 session slice 全走 `Router.dispatch` duck-typed）。
- `Ezagent.ExternalMirror.Adapter`（adapter.ex）— adapter behaviour；KIND 轴 `kind_of/1`（:394，默认 `:push`）、`delivery_discipline_of/1`（:411，默认 `:snapshot_refresh`）、`participation_profile_of/1`（:425，默认 `:read_only`）、`render/3` chokepoint（:440）。push-required callbacks（binding_module/target_ownership_check/event_to_payload）全在 `@optional_callbacks`（:368-384），per-kind 由 AdapterRegistry 运行期强制。
- `Ezagent.ExternalMirror.Gates`（gates.ex）— `run_all/4`（:102）按序：session-bind-cap → lookup_adapter → **check_adapter_bindable**（:133，`:pull` → `:not_bindable`）→ per-adapter-allow-cap → workspace-iso → target_ownership_check（bounded Task :294）。admin-wildcard 需 `action: :any`（:360-369）。
- `Ezagent.ExternalMirror.AdapterRegistry`（adapter_registry.ex）— adapter_id→module ETS；`register/1`（:87）在 RegistryLock 下 `insert_new` + `assert_no_binding_for_pull!`（:272）；`assert_required_callbacks!/1`（:182）按 kind 分叉（push/request_scoped 要 binding_module/ownership/event_to_payload :155-172，pull 要 render/live_topics/participation_profile :174-180 + discipline-specific）。
- `Ezagent.ExternalMirror.AdapterInstall`（adapter_install.ex）— event-driven 安装；`install/1`（:82）注册 per-adapter cap subject（`allow_<adapter_id>` :207）+ **仅 push** reconcile persisted bindings + spawn Worker。安装 hook deps 按 kind（pull 只等 `{AdapterRegistry, id}`，push 等 Adapter+Binding 两侧 :140-145）。
- `Ezagent.ActionSet.ExternalMirror`（behavior/external_mirror.ex:93）— Session Kind 上 bind/unbind/list_bindings；`use Ezagent.Lifecycle`；两步 bind flow 靠 FacadeNonceTable 32-byte nonce（forgery-proof）；`:external_mirror` slice 是 binding SoT，DB 表是 projection。
- `Ezagent.ActionSet.ExternalMirrorWorker`（behavior/external_mirror_worker.ex:91）— per-binding Worker Behavior；`:publish` self-dispatch `:cast` on `{:publisher_event, %Event{}}`；split-init。
- `Ezagent.Entity.ExternalMirrorWorker`（entity/external_mirror_worker.ex）— Worker Kind；persistence `:ephemeral`（:71）；`spawn_strategy {:custom, WorkerSpawn, :spawn_kind_server}`（:89）建两层 RootSupervisor→PerBindingSupervisor。
- `Ezagent.ExternalMirror`（external_mirror.ex，578 loc）— facade：`bind/4`（唯一跑 adapter I/O）、`unbind/3`、read helpers。`Ezagent.ActionSet.Publisher`（behavior/publisher.ex）— Publisher behaviour（4 callback history_retention/subscribe_from/snapshot/history），关闭 P11 escape；V1 唯一实现 `Ezagent.Entity.Session`（在 domain_session）。支撑：BindingRegistry/WorkerSpawn/BootReconciler/FacadeNonceTable/RegistryLock/WorkerRegistry/PerBindingSupervisor/RootSupervisor/Publisher.Event/Codec/SendKey。CLI：`Mix.Tasks.Ezagent.ExternalMirror.{Bind,Unbind,ListBindings,ListAdapters}`。

**易混淆点**：adapter KIND 三轴 `:push`（default）/`:pull`（customer feed/chat feed，render 按需、无 Worker、被 bind 拒）/`:request_scoped`（protocol-api，一 HTTP 请求一 binding）；adapter/binding 是 Grill-5 双向声明（编译期 assert）；`target_ownership_check/2` 是唯一许可的 adapter I/O，**禁 re-enter dispatch**（invariant #17）；`ExternalMirror.AdapterRegistry`（adapter_id-keyed）≠ `AgentBridge.AdapterRegistry`（flavor-keyed）。

**测试**：`test/` 33 个（external_mirror/* + behavior/* + invariants/*（10 条）+ mix/）。

### ezagent_domain_identity（domain / L2，core-team owned，非 pluggable）

- **entry**：`lib/ezagent_domain_identity/application.ex:62`；注册相位在 supervisor 前先 `register_identity_behaviors/0`（:395）+ `ConfigProjection.register/0`（:72）。supervisor 只挂一个 DynamicSupervisor `EzagentDomainIdentity.Application.UserSupervisor`（:74-76）。
- **拥有**：统一身份域 —— User Kind + CapBAC 数据槽（`:identity`）、统一 grant chokepoint、users/entity_tokens/profiles/invite/magic-link 表 + 认证 facade、per-agent 凭证与默认凭证源治理、agent-owned durable config 演进（ConfigStore/ConfigEvolve）+ CR 变更治理、membership-cap 授权三真相源、邮件配置 facade。

**Kind + 启动**
- `Ezagent.Entity.User`（entity/user.ex:1）— 唯一 Kind；`type_name :user`（:181），persistence `{:snapshot,:on_change}`（:216），`behaviors [Identity, UserCredentials, UserTokens]`（:207-213，**ApiKeys 已移出到 Agent Kind** :188-197）。`admin_uri/0`=`entity://user/system/admin`（:32）；`default_caps/1` 返 `[]`（:175，Decision #154 no-unowned-caps）。
- `EzagentDomainIdentity.Application` — admin 经标准 `Users.create/3` 幂等 provision（`ensure_admin_user/0` :245，`:test` 跳写 DB）；`register_identity_behaviors/0`（:395）把 Identity/IdentityAdmin 注册到 User+Agent、ApiKeys/CredentialGrant/ConfigEvolve/ConfigGovernance 到 Agent、UserCredentials/UserTokens/UserDefaultCredentialSource 到 User、WorkspaceUserAdmin/WorkspaceSharedCredentialSource 到 Workspace（全跨域注册，因这些 Kind 定义在别 app）。

**Identity ActionSet（caps 槽）**
- `Ezagent.ActionSet.Identity`（behavior/identity.ex:1）— `:identity` 槽持 `caps :: MapSet`，`use Ezagent.Lifecycle`（:73）。安全读 `:list_caps`/`:has_cap?`（:79/:87）+ **`:cascade_notify_managers`**（:104，`:cast`，cap-exempt :118，provenance-gate 只收 `caller: :vm_internal` :342，#161-B）。`create/1`（:142）建 caps + agent `add_agent_self_caps`（:175，注入 self-scoped `Sandbox.update_config`/`ConfigEvolve.reconcile_cascade` 支撑 config-evolve）。`activate/2`（:258）从 `users.caps_json` reconcile union。
- `Ezagent.ActionSet.IdentityAdmin`（behavior/identity.ex:350，同文件）— 特权写 `:grant_cap`/`:revoke_cap`（共享 `:identity` 槽，`data_owner→:no_owner`）。`handle_grant_cap/2`（:463）跑 `check_action_wildcard_grant_authorized` + `check_grant_authorized`（:652，授权者集 {self, admin, manager-of-target}；manager 分支 delegation-bounded）。`manages_target?/2` 委托 `Identity.Authority.holds_manage_over_target?/2`。

**统一 grant chokepoint（Decision #154）**
- `Ezagent.Identity.Grant`（identity/grant.ex:1）— 每个 grant/revoke dispatch 唯一入口。`prepare/4`（:225）是唯一派生 `granted_by` 处：从封闭 4-tag `{:held_by,:admin,:rule,:genesis}`（`@type authorization` :81-85，`derive/1` :246-273）派生，struct-overwrite `cap.granted_by`（:233）后校验必须 `%URI{scheme:"entity"}`（:235，fail-closed `{:granter_not_entity,_}`）。imperative wrappers `grant_cap/3`（:98）/`grant_cap_via_router/4`（:121）/`revoke_cap_via_router/4`（:158，at-join 补偿 revoke 必须 `:async` 防自死锁）；effect-constructor RAISE on error。**`{:system,...}` tag 已退役**（genesis collapse 2026-06-20，无 system:// principal）。
- `Ezagent.Identity`（identity.ex:1）— CapBAC 读 facade。`list_caps_for/1`（:25，经 dispatch，`await_ready` :69）；`grant_cap/3`（:211，shim 到 Grant）；`read_held_caps/1`（:275）；`read_entity_caps/1`（:336，不激活，live slice→snapshot fallback）；`caps_authorize?/2`（:297，非-dispatch READ facade cap-shape chokepoint）；`admin?/1`（:161，仅比对 admin_uri，UX gate 非安全边界）。
- `Ezagent.Identity.AdminAuthority`（identity/admin_authority.ex:1）— `admin?/2`（:108）= holds_admin_caps? ∪ holds_cross_workspace_admin_cap? ∪ home_is_system? ∪ member_of_system?。

**membership-cap 统一三真相源（#1172-1178）**
- `Ezagent.Identity.Authority`（identity/authority.ex:1，#161-B.1）— "P 是否对 X 有 manage/owner 权威" 单一真相源。`holds_manage_over_target?/2`（:69）、`workspace_admin?/2`（:88）；URI 版 `manages?/2`（:109，读 **durable identity caps** `Identity.read_entity_caps/1` 非 ctx.caps，= manage ∪ admin-union ∪ workspace-admin）。**owner-approval（#1178）的授权依据就是 `Authority.manages?/2`**。
- `Ezagent.Identity.Cascade`（identity/cascade.ex:1，#1177）— 有界反向权威扫描 + content-free notify。`managers_of/1`（:47，候选 → live caps + `granted_by_entity?` provenance filter → match，skip-self）；`notify_managers/2`（:79，body 只 `{uri,slice_key,cursor,event_at}` 无 cap 值）。触发端在 core `kind/cascade_hook.ex`。
- `Ezagent.Session.MemberReceive`（session/member_receive.ex:1，#1175，**住 identity 域**因 User/Agent 两 receive site 唯一共同依赖是 identity）— `authorize/1`（:78）读接收方 `ctx.siblings[:identity]` 预载 slice，`holds_member_cap_over?/2`（:102）按字段（`kind::session`+`action::receive`+concrete instance，不 pin behavior module）匹配自己持有的 member-cap。member-cap = `cap(:session,Session,:receive,S)`，走现有 grant chokepoint（#154 没变），旧 ephemeral bearer token 已删。

**凭证 / 授权 ActionSet cluster**
- `Ezagent.ActionSet.ApiKeys`（behavior/api_keys.ex:1，Allen 2026-05-26 从 User 翻到 Agent）— per-Agent 密钥槽 `:api_keys`。actions `:list_api_keys`(masked)/`:put_api_key`/`:delete_api_key`/`:get_api_key`(plaintext,cap-gated)。`data_owner/1`（:232）三级：`:creator_uri` 槽 → `AgentLineage` → `:no_owner`。
- `Ezagent.ActionSet.UserCredentials`（behavior/user_credentials.ex:1）— `:set_password`（`data_owner: :self`）。`Ezagent.ActionSet.UserTokens`（behavior/user_tokens.ex:1）— bearer token CRUD（`:mint_token`/`:list_tokens`/`:revoke_token`，均 `data_owner: :self`）。
- `Ezagent.ActionSet.WorkspaceUserAdmin`（behavior/workspace_user_admin.ex:1）— 挂 Workspace 的 `:create_user`（独立 cap subject 防 `Behavior.Workspace` 任意持有者铸用户，codex #356 CRIT）。`Ezagent.ActionSet.CredentialGrant`（behavior/credential_grant.ex:1）— 挂 Agent 的 `:revoke_credential_grant` + emit + `{:effect,{Terminable,:schedule_termination}}` 重启进 fail-loud auth。`Ezagent.ActionSet.UserDefaultCredentialSource`（behavior/user_default_credential_source.ex:1，#17）— `:set_default_credential_source`，owner 从 `ctx.self_uri` 派生绝不读 args（codex H1）。`Ezagent.ActionSet.WorkspaceSharedCredentialSource`（behavior/workspace_shared_credential_source.ex:1，#17 PR-3）。

**agent-owned config 演进 + CR 治理**
- `Ezagent.ActionSet.ConfigEvolve`（behavior/config_evolve.ex:1，rev4 化解 #607 confused-deputy）— agent 在自己 manage-cap 下改自己 config。actions `:apply_config_delta`/`:repoint_config`（manage-cap gated）/`:read_cascade`/`:reconcile_cascade`（self-cap boot self-heal `:cast`）。step-1 durable 写 ConfigStore 再 emit deferred dispatch。`assert_subject_self/2`（:371，CE-1 self-bind）。
- `Ezagent.ActionSet.ConfigGovernance`（behavior/config_governance.ex:1，rev3）— Agent Kind CR 治理。7 actions open_cr/stage_item/unstage_item/preview_cr/publish_cr/reject_cr/rollback_cr（全 manage-cap gated）。
- `Ezagent.Socialware.ConfigStore`（socialware/config_store.ex:1，2026-06-11 从 socialware 迁入，**模块前缀仍 Ezagent.Socialware.* 但物理住 identity app**）— 不可变 config 对象 + high-layer cascade 指针。唯一公开 durable 写 `write_and_point/1`（:56）。`ConfigChangeStore`（config_change_store.ex）CR 生命周期引擎（双层幂等 publish）；`ConfigProjection`（config_projection.ex，#607 consume 半，`:socialware_config_dir` UriQuery resolver）；`Ezagent.Socialware.ContentHash`（content_hash.ex:1，**#1173 新**，`of/1` :27 递归 key-sorted → SHA-256，单算法源，session 域 `Definition.content_hash/1` 委托此）。schemas：ConfigObject（`content_hash` 列 :22 + `unique_constraint(:source_turn_id)`）/ConfigPointer/ConfigChangeRequest/ConfigChangeItem。

**注册 / 认证 / provisioning cluster**
- `Ezagent.Users`（users.ex:1）— `users` 表 facade。`create/3`（:81，reject `anon-` 前缀）、`create_read_only/2`（:167，匿名 viewer，无密码无 default_caps，`confirmed:false`）、`delete/1`（:208，先 `Lifecycle.destroy` 拆 Kind）、set_password/verify_password(Bcrypt)。`confirmed`（:42）是 anon-ness 真相源。
- `Ezagent.Entity`（entity.ex:1，PR#142）— 统一认证 facade。`authenticate/3`（:57）user→bcrypt / agent→token；`spawn_principal/1` + `ensure_spawned/1`（demand-spawn hydrate caps_json）。`Entity.Token`（entity/token.ex，`esr_pat_` 前缀）/`Entity.Profile`/`Entity.InviteCode`/`Entity.MagicLinkToken`（15-min，SHA-256）。`Registration`（registration.ex，email-magic-link 注册协调，invite_code 或 open_self_serve 两模式）。
- `Ezagent.AppSettings`（app_settings.ex:1）— `app_settings` KV facade。`smtp_config` 双 shape（SMTP relay / `email.ezagent.chat` REST）；`smtp_configured?`/`mail_api_configured?`（:76，**#1198 新**）/`mail_configured?`/`registration_open?`。`Ezagent.Mail.SmtpOpts`（mail/smtp_opts.ex，`smtp_config`→Swoosh SMTP opts，465→SSL/587→STARTTLS）。
- 迁移工具：`Identity.GrantMigration`（chat→session cap 重写）、`Socialware.ConfigKeyRename`（`advisor.behavior`→`agent.soul`）；mix tasks `ezagent.invite` / `ezagent.session.migrate_grants` / `ezagent.user.*`（多数 DEPRECATED 指向 `mix ezagent workspace/user`）。

**易混淆点**：本 app 全部 ActionSet（11 个定义，IdentityAdmin 与 Identity 同文件）**100% `use Ezagent.Lifecycle`**；唯一 Kind 是 User，Agent/Workspace Kind 在别 app（boot 时跨域 REGISTER）；grant 唯一 chokepoint = `Grant.prepare/4`；file 名 vs module 名不一致（`behavior/*.ex` 里是 `Ezagent.ActionSet.*`；`member_receive.ex` 是 `Ezagent.Session.MemberReceive` 却住 identity app）；socialware 命名残留（ConfigStore/ConfigObject/… 前缀仍 `Ezagent.Socialware.*` 物理住 identity）。

**自 de4f40a5（5 commit）**：#1172（grant.ex +37 member-cap grant-at-join）、#1175（新 member_receive.ex）、#1173（新 content_hash.ex + config_object content_hash 列）、#1177（identity.ex ±153 + 新 authority.ex/cascade.ex）、#1198（app_settings.ex +33 REST mail 就绪检测）。

**测试**：约 41 个 `*_test.exs`（behavior/ 19、identity/ 5、entity/ 5、socialware/ 2、root 若干）。

### ezagent_domain_pty（domain / L2）

- **entry**：`apps/ezagent_domain_pty/lib/ezagent_domain_pty/application.ex:32`（Registry + DynamicSupervisor）；facade `Ezagent.Domain.Pty`（pty.ex:45 `start/2`）。deps 只 core + erlexec。**自 de4f40a5 无 lib 变化**。
- **拥有**：PTY-managed sidecar runtime（一 Server per agent_uri，erlexec `:exec.run/2` 带真 tty）+ `:write` ActionSet + 数据驱动 auto-prompt scanner + auth-failure observer + 三相 phase 状态机 + stdout snapshot/redraw。
- `Ezagent.Domain.Pty.Server`（server.ex）— `cmd_override` 接 LIST（argv/execve 无 shell，build_exec_cmd/2 :563）或 STRING（legacy `/bin/sh -c` :570），nil raise（:572）。命令大小 backstop `@packet2_limit 65_535 - @command_size_headroom 4_096`（:535，防 erlexec `{packet,2}` node-wide crash）。auth-failure observer emit-only（scan_auth_observers/2 :701，绝不 send stdin）；auto-prompt scanner（scan_auto_prompts/1 :745）。
- `Ezagent.ActionSet.Pty`（behavior/pty.ex:107）— `:write` ActionSet，`use Ezagent.Lifecycle`（no-transients，durable write_calls/total_bytes 计数）。`handle_write/2`（:139）经 `ctx.self_uri` → `Domain.Pty.lookup/1` → `Server.write_input/2`。Kind↔Behavior 绑定注册在 session/instance-message 域（Agent Kind 定义处，**unverified**——本次未现读那处）。
- `Ezagent.Domain.Pty.AutoPrompts`（auto_prompts.ex）— 纯数据 catalog，`default/0`（:26）返 **7 个** well-known claude 启动对话 auto-prompt：theme_dialog/login_method_dialog/dev_channels_dialog/claude_md_external_imports_dialog/trust_folder_dialog/mcp_trust_dialog/bypass_permissions_dialog。
- **⚠ PTY crash-bug 现场（forensic，精确行号）**：erlexec 按任意字节边界投递 stdout，PTY buffer 是含 ANSI 的原始字节，可含被切断的多字节 UTF-8（CJK）。三处热点：`server.ex:816`（trim_buffer_only/1 `binary_part(buf, byte_size-16*1024, 16*1024)` 裸字节切片）、`server.ex:313`（snapshot_buffer/2 同样裸字节尾切）、`server.ex:794`（normalize_ws/1 `String.replace(s, ~r/\s+/u, " ")` **unicode `/u` regex**，遇非法 UTF-8 raise → PtyServer crash → PTY 死）。触发链 `handle_info({:stdout,..})` :648 → buffer :663 → scan_auto_prompts :671 → AnsiStrip.strip :746 → matches?/2 :786 → normalize_ws :794。（因果链据现读推断，实际 raise 未跑测试验证——**unverified**，三处行号本身现读确认。）
- **易混淆点**：本 app 无 Kind（host 一 ActionSet 到外部 Agent Kind）；phase topic `"pty:phase:" <> uri`（与 Domain.Python 故意同名）。
- **测试**：11 个（含 e2e/category_07_pty_test.exs）。

### ezagent_domain_python（domain / L2 runtime）

- **entry**：`apps/ezagent_domain_python/application.ex:27`（Registry + DynamicSupervisor）；facade `Ezagent.Domain.Python`（lib/ezagent/domain/python.ex）。**自 de4f40a5 无 commit**。声明 NO Kind/ActionSet/flavor/recipe，socialware 无关。
- **拥有**：ezagent 经 uv 启动的 Python 子进程 OS-process 生命周期 —— 一 Server per canonical handle，LSP-framed JSON-RPC 2.0 over stdio（非 PTY、非 cc/MCP-bridge、非 CapBAC 边界）。
- `Ezagent.Domain.Python`（python.ex）— facade：`start_subprocess/1`（同步阻塞到 `python.ping` pong :144）、`call/4`（:213）、`notify/3`（:235）、`stop/1`、`alive?/1`、`handle_key/1`（canonicalization chokepoint :48，非 URI/非 binary raise；非-canonical binary raise；`"test://"` 前缀 identity 放行 :51）。
- `Ezagent.Domain.Python.Server`（server.ex）— GenServer 包 `:exec.run/2`；**readiness 同步在 `init/1` 内**（do_spawn_and_ping/1 spawn+ping gate 在返回前 :186）；per-call timer + cancel/flush late-result；RPC timeout → unhealthy tear-down（:519）；三相 phase 广播（:starting→:running 在 ping pong 后 :209）；bounded 2-file rotating stderr；pid-file namespace `"python"`。
- `Ezagent.Domain.Python.AgentLifecycle`（agent_lifecycle.ex）— py/np flavor 复用的 subprocess-Behavior helper（ensure_alive/1 :61、subscribe_phase/1、phase_topic/1、phase_from_signal/1），**零 Template 反向引用**。`Spec`（spec.ex）输入 struct + validate/1 + to_argv/1（显式 argv / `:uv_script` / `:uv_project`，uv 经 `System.find_executable` :158）。`FrameBuffer`（LSP-frame 增量解析）+ `JsonRpc`（envelope 编解码）。
- **易混淆点**：preflight（Spec.validate + to_argv）在 caller 进程跑，不建半启子进程；并发 start 在 `:via` Registry 塌缩，loser 走 `await_ready/2`。
- **测试**：9 个（server/json_rpc/integration/frame_buffer/spec/handle_key/server_phase/agent_lifecycle/boot）。

### ezagent_domain_session（domain 层，socialware 生命周期核心所在）

- **otp_app**：`:ezagent_domain_instance_message`（历史名，目录已改名 domain_session，模块前缀仍 `EzagentDomainInstanceMessage`）。**entry**：`apps/ezagent_domain_session/lib/ezagent_domain_instance_message/application.ex:62`。**LOC**：78 个 .ex 共 24560 行；最大 4：membership.ex(1273)、session.ex(1156)、orchestrator/tools.ex(941)、application.ex(896)。
- **拥有**：统一 `Ezagent.Entity.Session` Kind（chat + socialware 一个参数化 Kind）、SessionCreator materialization pipeline、成员/准入/member-cap 安全机器、**整条 socialware 生命周期 author→publish→discover→install→use**。

**socialware 生命周期五段（全在此 app）**
- **author** `Ezagent.Socialware.ManifestResolver`（manifest_resolver.ex）— fail-closed 边界，config-authored manifest（string name-refs uses/views）→ runtime `Definition`。`resolve/1` :14；`ensure_plugins_installed` 校验 uses[] 已装 :41-44；view name-ref → backing ActionSet via `Ezagent.UI.SessionViewRegistry` ETS :73-100，再 `Definition.new/1`。
- **publish** `Ezagent.Socialware.ConfigGovernance.Socialware`（config_governance/socialware.ex）— socialware Definition 是受治理的 ConfigChange subject，自带 cap `cap(:socialware,:manage,:any,socialware:<name>,ws)`（manage_cap/3 :24-37）。`open_cr/2`(:41)→`stage_definition/3`(:60)→`publish_cr/2`(:81)/`reject_cr/2`(:187)。**`publish_or_upgrade/2`（:114-132，#162/#165）** 共享幂等 RULE（P0 §5）：查 `DefinitionRegistry.lookup`，content-hash 相同 `:exists`、不同走 `publish_new_revision(:upgraded)`、无则 `:published`（:134，跑完整 open_cr→stage→publish 保 admin 门+审计）——**Demo.Hello.publish 的调用入口**。PUBLIC scope admin 门 `publish_cr` 调 `authorize_public_scope/2`（:197 → `authorize_admin` :228，`AdminAuthority.admin?` 否则 `:public_socialware_requires_admin`）。跨 ws 隔离 `assert_workspace/2`（:250，`:cross_workspace_socialware_cr_denied` :254）。
- **discover** `Ezagent.Socialware.DefinitionRegistry.list/1`（definition_registry.ex:262，cross-workspace 可见性过滤 + retract 过滤 + 每 name 去重）。
- **install** `Ezagent.Socialware.Installation.install_template_installs/4`（installation.ex:177，写 per-session install 记录，freeze-pin 到当前 revision）。
- **use** `SessionCreator.TemplateTeam → DefinitionAgents.materialize_definition_agents/4`（definition_agents.ex:64，只物化 `roles` 里 `fill==:agent` 的槽，template_team.ex:33-40 先 `agent_role_slot?` 过滤 :48）。

**Definition（config-as-data，非 live Kind）**
- `Ezagent.Socialware.Definition`（socialware/definition.ex，487 行）。存为 ConfigObject，OPAQUE 非-URI subject `socialware:<name>`，key `"socialware"`，workspace 是独立 ConfigStore 字段（definition_registry.ex:37-40）。
- **字段全集（17 个，defstruct :12-28）**：name/version/title/description/uses/bases/shape/views/**roles**/assets/routing_rules/prompt_templates/legends/orchestrator_template_uri/adapters/visibility_policy/owner_policy。`visibility_policy` 默认 `%{publish_policy: :auto, web_anon_access: false, scope: :private}`（:27）；`owner_policy` 默认 `%{type: :installer}`（:28）。
- **flavor 不是顶层字段** —— 在 `roles` 每个 agent 槽携带（`@type role_slot` :34-36：agent 槽 `%{role_name, fill: :agent, recipe, flavor}` 三者必填非空 role_slot/1 :284-286；human 槽 `%{role_name, fill: :human}` :291-296，运行期分配不在建 session 时物化）。字符串键等价。
- `behaviors/1`（:117-125）派生 spawn set = `[Session] ++ views ++ shape ++ (bases minus Session)`；views 必须进 spawned set 好让每个 view 的 `<sw>_render` cap 注册。
- **role-slot 三个 enforce 点（`new/1` with 链 :70-83）**：(1) `reject_retired_declaration_fields/1`（:313-318，attrs 含 `agents`/`members` 键 → `{:retired_socialware_definition_field, field}`）；(2) `reject_participant_instance_uris/2`（:323-366，调 :82，递归扫 roles+routing_rules，任一字符串 parse 成 `entity://…/agent|user/…` via `URI.type?` :358-366 → `{:socialware_definition_declares_instance_uri, offenders}`）；(3) `owner_policy/1`（:412-425，`:fixed` owner 被拒 `:socialware_definition_declares_owner_uri`，只准 `%{type: :installer}`）。
- `content_hash/1`（:168-170）委托 `Socialware.ContentHash.of/1`（env-independent）；`owner_uri/2`（:176）installer-derived（caller 派生 owner）。

**DefinitionRegistry（P0 版本化/可信安装 + #165 admin 门）**
- `Ezagent.Socialware.DefinitionRegistry`（socialware/definition_registry.ex，538 行）。retract 独立 key `@retract_key "socialware_retract"`（:24，不进 content-hash，retract/restore 身份存活）；`retracted?/2`（:154）、`set_retracted/4`（:179）。`resolve_installable_revision/3`（:87，按精确 revision-id content-hash 安装，硬 scope 门 `authorize_installable_scope` :115 own-ws/system/public via `visible_to_workspace?` :398 ∧ `refute_retracted` :128 ∧ 可选 `verify_content_hash` :136）。`write_definition/2`（:235，`authorize_definition_write` :423 拒跨 ws `:cross_workspace_socialware_definition_write_denied` :441 + **`authorize_public_scope_write/2`（#165 domain chokepoint 门 :462，`:public` scope 写要求 `AdminAuthority.admin?` 否则 `:public_socialware_requires_admin`；`:system_seed` 豁免、PRIVATE 写不触）**）。`seed_builtin_definitions/0`（:201，boot seed `chat`+`socialware` 进 `workspace://system`，socialware builtin 声明 `external_feed` customer adapter :296 + web_anon_access:true :297）。`lookup/2`（:45，caller-ws-first → system → public fallback，retract-aware）。

**Installation（freeze-pin + 匿名两层 web gate）**
- `Ezagent.Socialware.Installation`（socialware/installation.ex，581 行）。freeze-pin：`freeze_template_installs/2`（:93，建 session 前把 install resolve 到当前 revision-id）；`install_template_installs/4`（:177）；`repoint_template_installs/4`（:197，唯一显式升级路径）；`resolve_install` pin-honoring（:465）。**匿名两层 web gate**：`web_anon_access?/1`（:226，决定是否 mint anon）；`anon_view_caps/1`（:256，每个 PUBLIC install 的每个 view-action mint 一个 `<sw>_render` cap，private install 不贡献 cap → `SessionView.authorize_view` 拒）。granter = session owner，fallback admin（:293-298）。（旧报告 :117/:147 已迁移到这些新行号。）

**Conformance（12 断言）**
- `Ezagent.Socialware.Conformance`（socialware/conformance.ex，335 行）。`check/2`（:36）按序跑 12 条（assertions/0 :63-77）：uses_plugins_installed / bases_shape_load / views_exist_caps_registered / agent_recipes_resolve / agent_caps_and_role_uniqueness / adapters_registered / orchestrator_uri_parses / install_resolves / template_installable / **routing_receivers_resolve** / prompt_template_refs_valid / view_caps_gate_ready。routing receiver 只准解析成已声明 role 名（check_routing_receivers :268，否则 `:socialware_receiver_not_a_role` :276）。`mix ezagent.socialware.check` 是薄 wrapper。

**SessionCreator DefinitionAgents materialize（#1180/#1185/#1194 后）**
- `DefinitionAgents.materialize_definition_agents/4`（session_creator/definition_agents.ex:64）— 把 agent 角色槽 materialize 成 live SPAWNED 成员。role_name 批内唯一 FIRST（:79-80），已有同 role live 成员则幂等跳过（existing_member_for_role :101）。**#1194 install-mode 分叉**（materialize_one :97 → install_mode_of :385）：`:fresh` → materialize_fresh_agent（:118，`planned_agent_uri` 新 UUID 实例 URI :347、`create_agent_from_recipe` recipe×flavor :222、faceted `session.join` 带 `%{role_name}` cleanup-on-fail :246、grant recipe caps LAST :134/:303）；`:reuse` → reuse_existing_agent（:139，复用已存在 agent URI，`ensure_reuse_recipe_match` :158 校验 recipe 一致，走 `Participants.add_participant` :143）。flavor 缺省 `"cc"`（flavor_of :404-409）；`lookup_ref` 剥单个 `recipe:` 前缀 :205。system-mediated：spawn under owner，join/cleanup under admin inline least-priv。
- `TemplateTeam.materialize_template_team/4`（session_creator/template_team.ex:9）— 装 prompt_templates/legends/rule_sets 后调 materialize_definition_agents，只传 `fill==:agent` 的槽（:39）；`provision_declared_member/4`（:56）显式拒 human 槽（`:human_role_slot_requires_runtime_assignment` :66）和 agent 槽（`:agent_role_slot_materialized_at_session_create` :72）。

**统一 Session Kind + 准入/member-cap 安全机器**
- `Ezagent.Entity.Session`（entity/session.ex，391 行）— ONE parameterized Kind（P5-1b substrate collapse）。`behaviors/0`（:56）声明 UNION superset（Session, Publisher.SessionImpl, ExternalMirror, Turn, Surface, SupervisorApproval）；per-instance ACTIVE subset 经 `:kind_base` 穿（`chat_behaviors/0` :73 = {Session,Publisher,ExternalMirror} vs `socialware_behaviors/0` :88 = {Session,Turn,Surface,SupervisorApproval,Publisher}）。`requires_explicit_behavior_set?` true（:106）；persistence `{:snapshot, :on_change}`（:98）；`owner/1`（:299）读 `:session` slice `:owner_uri`。
- `Ezagent.ActionSet.Session`（behavior/session.ex，1156 行）— `use Ezagent.Lifecycle`（:117）。**#1178 admission actions cap-exempt+in-handler authz**：`:approve_admission`（:276）/`:deny_admission`（:286）/`:withdraw_admission`（:296）；`cap_exempt_actions/0`（:314）；handler :888/:894/:899 委托 Membership。`pending_members: %{}` slice 初值（:357）。**#1194 `action(:assign_role)`（:187）/ `handle_assign_role`（:808）**（member edge 上分配 human role，要求 user URI :831）。
- `Ezagent.ActionSet.Session.Membership`（behavior/session/membership.ex，1273 行）— `do_join/5`（:28）唯一 chokepoint。**#1178 门** `admission_pending?/2`（:123）四条 AND（:137-141：未 mount ∧ 非 :user 类型 ∧ caller 真 bare principal ∧ caller 不控制 member）→ `record_pending_admission`（:227，记进 `:session` slice `:pending_members`，entry `%{requested_by, requested_at, request_ref}` :232，不授 cap/不 mount），`notify_pending_managers`（:261）content-free 通知 member 的 managers。**放行** `caller_controls_member?`（:170）= `Identity.Authority.manages?/2` ∪ `holds_spawned_by_authority?`（必须限死 `{:spawned_by,caller}`，:189，放宽会重开 threat X）。**审核** `approve_admission/3`（:326，`manages?` gate :334→重跑 do_join，drop pending 作 effect :343 所以 raise 整体 abort）/`deny_admission/2`（:358）/`withdraw_admission/2`（:380，requester 自撤）。也含 join cap 供给 + 参与 tier `mount_participation_caps`（:1175）+ leave/remove_participant（:869/:948）。
- **#1172 member-cap `Ezagent.ActionSet.Session.MemberCap`**（behavior/session/member_cap.ex，297 行）— `grant_at_join/2`（:31，在 do_join 非-pending 分支 membership.ex:88 授 universal base `cap(:session,Session,:receive,S)` 进成员 `:identity` slice，member_cap/2 :267）；**强制 `:async`**（:60-65，同 Session Kind re-enter 会 sync 自死锁）；EXACT 5-tuple idempotency `holds_member_cap_exact?/3`（:287）；`revoke_at_join`（:94，async 补偿）；`revoke_membership`（:139，leave/remove 的 CHECKED sync revoke）；`member_snapshot_caps`（:237，从 SnapshotStore 读避免 cross-Kind stall）。
- **#1175 receive 读 held member-cap**：`behavior/session/delivery.ex:182` fan-out 时 `caps: MapSet.new()`（删了旧 ephemeral bearer token :179-181）；授权改由接收方持有的 member-cap 决定（`MemberReceive.authorize/1` 住 identity 域）。

**role-slot 其余适配（#1180/#1185）+ config_fork**
- `Ezagent.Socialware.DefinitionEditor`（socialware/definition_editor.ex，626 行）— `config_for_template/2` 单一 SoT 读；`member_declarations_for_template`→改名 `role_slots_for_template/2`（:80，旧名 :89-92 deprecated delegator）；live snapshot `merge_live_config`（:459）把 live 成员反投影成角色槽（live_role_slots :501 / live_member_role_slot :509）。`DefinitionSync`（orchestrator/tools/definition_sync.ex）成员回写进 `definition.roles`（role_slot_for_member :129）。`Migration`（orchestrator/tools/migration.ex）cutover 读 `roles` 回退 `members`。
- **hello 黄金样板 `Ezagent.Socialware.Demo.Hello`**（socialware/demo/hello.ex，168 行）— `manifest_attrs/1`（:76）单一 SoT（roles 用 `%{role_name, fill:"agent", recipe:"np", flavor:"py"}` :97-99，views `["hello_render"]` :96，scope:public/publish_policy:supervised/web_anon_access:true :117-121）；`publish/0`（:133）DOGFOOD 真 governance，经 **`Governance.publish_or_upgrade/2`（:139，幂等 :exists/:upgraded/:published）** 进 `workspace://system`；`published?/0`（:148）。**boot 调用点在 `ezagent_plugin_hello` Application，不在本 app**。
- `Ezagent.Session.ConfigFork.fork_config/3`（session/config_fork.ex:93，227 行）— config-only（Invariant #10 无 message history），真授权门是 workspace `:create_session` cap 原样透传进 `Ezagent.Workspace.create_session`（create_session/5 :167）；跨 workspace fail-closed `:cross_workspace_copy_unsupported`（:156）。

**易混淆点**：Definition ≠ live Kind（config-as-data，OPAQUE subject 非 URI）；`agents`/`members` 字段已退休（#1180，只有 `roles`）；**两个 socialware app** —— lifecycle CORE 全在本 app，`ezagent_domain_socialware` 只是 customer-feed/anon-User substrate；member-cap grant 必须 `:async`、revoke-on-leave 必须 `:sync`；admission `{:spawned_by,caller}` 门 load-bearing。

**自 de4f40a5（14 commit）**：#1194（role-slot install fresh/reuse 分叉 + session.assign_role）、#1185（de-bake role，agent role-agnostic，role_name 只活成员边，删 dead SessionAgentMaterialize）、#1184（修 leaked `Ezagent.Behavior.Workspace`→`Ezagent.ActionSet.Workspace`）、#1182/#165（DefinitionRegistry.authorize_public_scope_write domain 门 + caller caps 穿线）、#1180（role-slot P1，agents/members→roles，三 enforce 点）、#1171（credential-status view）、#1178（admission gate 闭 threat X）、#1177（cascade notification）、#1175（atomic receive cutover）、#1176（content-hash-addressed install）、#1173（registry P0 versioned + freeze-pin + retract + owner_policy）、#1172（member-cap foundation）、#1168（hello orchestrator-fronted routing）。

**测试**：**130 个 .exs**：e2e/(4)、ezagent/socialware/(15)、ezagent/session/(admission ~18)、ezagent/behavior/(~20)、ezagent/entity/(~13)、integration/(~55，含 definition_agents_materialize/role_slot_two_session_acceptance/session_config_fork/repair_freeze_pin/recipe_responsibility_lockin)、invariants/(template_fork_lineage)。

### ezagent_domain_socialware（domain tier — customer-feed / anon-User substrate，非生命周期核心）

- **entry**：`lib/ezagent_domain_socialware/application.ex:19`。监督树**唯一 child** `Ezagent.Socialware.AnonUser.Sweeper`（:38）。boot 后 `register_adapters/0`（:51）注册两个 `:pull` adapter：`ChatFeedAdapter` + `ExternalFeedAdapter`（:16,52）。**无 session supervisor/Kind**。
- **拥有**：customer-feed / settlement 投影 substrate + anon-User（匿名只读访客）铸造/准入/GC + PageView 投影。**不拥有 session Kind**（P5 substrate collapse 后 socialware/advisor session 是统一 `Ezagent.Entity.Session` 的实例，注册在 domain_session）。
- `Ezagent.Socialware.ExternalFeed`（external_feed.ex，**514 loc**）— participatory + durable delta-cursor replay over DeliveryOutbox settlement outbox。`snapshot/2`（:47）、`join/3`（:164，**从 0 replay 全 backlog** `committed_deliveries_since(session_uri, 0)` :182）、`replay/3`（:205）、`committed_deliveries_since/2`（:116）、`latest_cursor/1`（:273）。attachment 授权 approved_attachment?/2（:300）+ authorized_attachment_path/4（:357）。external page 从 **COMMITTED** surface 版本渲染（external_page/1 :462 + committed_surface_version/1 :478，按 committed_seq 序、防 leak approved-but-uncommitted）；cold-safe surface 读（surface_slice/1 :499 掉 StateRebuilder）。`chat_messages/2`（:94，#1168 加，返 FULL collaborative chat 供 hello whiteboard）。
- `Ezagent.Socialware.ChatFeed`（chat_feed.ex，146 loc）— CHAT session 外部 SPA 视图，**windowed snapshot-refresh**（无 cursor、掉 advisory 自愈）；`snapshot/2`（:119，latest-N external_visible）、`chat_tree/1`（:72）、`topic/1`（:102）。history_limit 200。
- `Ezagent.Socialware.ExternalFeedAdapter`（external_feed_adapter.ex）— `:pull`/`:cursor_replay`/`:participatory`，wrap ExternalFeed；cap-only `.Allow`（`:allow_external_feed`，`use Ezagent.Lifecycle` + `dispatchable? false`）。`Ezagent.Socialware.ChatFeedAdapter`（chat_feed_adapter.ex）— `:pull`/`:snapshot_refresh`/`:read_only`，wrap ChatFeed；cap-only `.Allow`（`:allow_chat_feed`）。**moduledoc 明标（:66-73）**：advisor vertical 退役后无插件声明它 → `allow_chat_feed` cap subject 不再 boot-published，live chat feed 直接调 ChatFeed。
- `Ezagent.Socialware.ChatFeedAuth`（chat_feed_auth.ex，81 loc）— caller-identity token（Phoenix.Token 签 `{caller_uri, session_uri, expires_at}`）；`issue_token/3`（:28）/`verify/2`（:50）。**只证服务端为该 caller 铸过 token，NOT 授权**——membership 每次 join/replay live 重查。
- `Ezagent.Socialware.AnonUser`（anon_user.ex，203 loc）— 匿名只读 User flavor；`mint/1`（:65，empty caps_json）；`mint_for_public_session/1`（:118，#51 §4.1 匿名接入 chokepoint / Decision #154）gated on `PublicView.web_anon_access?`，born WITH 窄 concrete-instance `session.join` cap（join_cap/1 :152，granted_by session owner/admin fallback :164）+ `Installation.anon_view_caps` view read-caps（:131），不走 Grant、无 system:// principal。`AnonAdmission`（anon_admission.ex，131 loc，`admit_anonymous_participant/2` :31 / `refresh_anonymous_participant/3` :54，join 经 `Ezagent.Invocation.dispatch` :103 P14）。`AnonBinding`（anon_binding.ex，493 loc，Ecto `socialware_anon_bindings`，`workspace_uri` DERIVED from session invariant #14；`touch/3` :120 fail-closed；anon→login merge FSM `claim_for_merge/3` :335 + `mark_merge_state/2` :394，6 态 claimed→…→verified）。`AnonUser.GC`（gc.ex，337 loc，48h-TTL reaper，非破坏 claim → cast leave → **confirm member removed** member_removed?/2 :206 → Users.delete → verify → hard-delete binding LAST；leave 用 admin inline cap :293，#154 删了 `system://socialware-gc`）。`AnonUser.Sweeper`（sweeper.ex，hourly tick 调 GC.sweep）。`PublicView`（public_view.ex，22 loc，`web_anon_access?/1` delegate 到 Installation，fail-closed）。
- `EzagentDomainSocialware.PageView`（page_view.ex，191 loc）— `Ezagent.UI.SessionView` impl（`id :page`）；`applies_to?/1`（:24）keys on `:surface` slice；`external_render/1`（:66）从 **live** surface slice 走 `Surface.external_tree`（**注意** committed-version 渲染在 `ExternalFeed.external_page` 不在这里）。

**易混淆点**：两个投影两读模型（ExternalFeed = participatory + cursor-replay；ChatFeed = read-only + snapshot-refresh），都注册为 `:pull` adapter，都由 caller 的 Phoenix channel（`SessionFeedChannel` 在 ezagent_web）驱动；**单一安全边界**——每次外部读经共享 `Ezagent.Session.Membership.authorize/3`（3 参含 session_uri，#1175 后要求 caller HOLD 住 session member-cap，external_feed.ex:402 / chat_feed.ex:137 / gc.ex:212）；anon-User 构造性只读（`Users.create_read_only` 跳过 default_caps）；**stale 注释**——socialware mix.exs deps 注释引用不存在的 `CustomerFeedAdapter`（实际是 ExternalFeedAdapter + ChatFeedAdapter，标记供核对）。

**自 de4f40a5（4 commit 触及、3 lib 真改）**：#1175（**唯一改 security 语义**：三处 `Membership.authorize` 加第 3 参 session_uri，revoked ex-member 即便有陈旧 roster 也被拒）、#1168（ExternalFeed.chat_messages/2 供 hello 共享白板）、#1180+#1173（仅改 test，真代码在 domain_session）。net：external_feed 486→514 loc，gc 333→337，chat_feed authorize 签名变。

**测试**：`test/` 37 个（anon_* / chat_feed* / external_feed* / external_page_commit_gate / membership_predicate / outbox / settle_crash_matrix + behavior/socialware_publisher_read + page_view + integration/7）。

### ezagent_domain_ui（domain / L2；声明 NO Kind NO ActionSet）

- **entry**：`apps/ezagent_domain_ui/lib/ezagent_domain_ui/application.ex:30`（空 children，仅 `register_session_views/0` :52 注册 3 个 builtin view）。**自 de4f40a5 无 lib 变化**。deps core + pty + identity + external_mirror，**无 ezagent_web 依赖**。
- **拥有**：无状态 Phoenix.Component 库（primitives/components + nested-shell chrome）+ **SessionView 扩展点契约 + ETS registry**（让任何 app 插 per-session view tab，含 T2-2b per-view cap 授权门）+ caller-authorized 读 helper（在 dispatch 之外自己强制 workspace 隔离）。boot 零 GenServer。
- `Ezagent.UI.SessionView`（session_view.ex）— Phoenix.Component `@behaviour`。callback id/label/icon/applies_to?/render（必），external_render?/external_render/view_behavior 为 `@optional_callbacks`（:99）。**统一 per-view cap 授权门 `authorize_view/3`（:121，T2-2b）**：声明 `view_behavior/0` 的 view → cap-gated（caller 须持匹配 render cap），无 view_behavior → true。`render_needed_caps/2`（:155，形状与 `Installation.anon_view_caps` 一致）。
- `Ezagent.UI.SessionViewRegistry`（session_view_registry.ex）— ETS `:ezagent_session_view_registry`。`init/0`（:41 幂等）、`register/1`（:54）、`applicable_views/1`（:70 无 caller 门）+ `applicable_views/2`（:92 caller-aware 走 authorize_view/3）、`external_renderers/1,2`、`lookup/1`。**属主在此，消费方 #1199 在 world**。
- 3 个 builtin view（application.ex:54-62）：`EzagentDomainUi.Pty.TerminalView`（id :pty，applies_to?/1 逐个问 `Domain.Pty.alive?/1` cross-flavor 检测）、`Routing.RoutingView`（id :routing）、`ExternalMirror.View`（id :external_mirror，applies_to?/1 → true）。
- chrome：`IdeShell`（Tier-2 OUTER，764 行）、`WorkspaceShell`（BODY，292）、`AdminShell`（226）；atom 库 `Primitives`（838，含 uri_picker :683）、`Components`（469，shadcn-inspired）。caller-authorized 读 helper：`Ezagent.UI.UriOptions`（uri_options.ex，每公共函数取 caller URI 自解析 workspace 授权，无 cross_workspace 逃生口，复用 `Capability.workspace_of/1` 与 dispatch step 5.6 同源）、`Ezagent.UI.CommandSource`（CmdK 纯排序函数，无 Router/KindRegistry 依赖避反向依赖）、`AutoDerive`（从 Kind+Behavior 内省派生 listing/detail）。PTY 渲染 `Pty.Terminal`（xterm）+ `Pty.TerminalSeam`（host-LV seam，`dispatch_input/3` 经 `?action=pty.write` 走 `Invocation.dispatch`）。`EzagentDomainUi.Gettext`（Tier-2 自有 backend，en + zh_CN）。
- **易混淆点**：`Ezagent.UI.SessionView`（UI 侧 Phoenix.Component behaviour）命名刻意区别于 dispatch 侧 `Ezagent.ActionSet`；无 socialware lifecycle 代码（只 docstring 引用 Definition.visibility_policy/Installation.anon_view_caps 作 anon-mint 对侧）。
- **测试**：13 个（admin_shell/authorize_view/command_source/ide_shell/pty/routing_view/session_view_registry/uri_options/uri_picker/workspace_shell）。

### ezagent_domain_workspace（domain / L2）

- **entry**：`apps/ezagent_domain_workspace/lib/ezagent_domain_workspace/application.ex:32`；`boot_complete/0`（:64 → `Loader.load_all/0`，由最后启动的 app 调）。deps core + agent + identity。
- **拥有**：Workspace Kind（`workspace://<name>`）+ cluster shape（member set / session·agent 模板注册 / workspace-scoped routing rule / magic-link 规则 / P9 responsibility 分配）+ 统一 provisioning（create_agent/create_session/create_user），持久到 SQLite `workspaces` 表（唯一 SoT）。
- `Ezagent.Entity.Workspace`（entity/workspace.ex）— Kind，`type_name :workspace`（:50）、`behaviors [ActionSet.Workspace]`（:53）、persistence **`:ephemeral`**（:61，`workspaces` 表是 SoT 非 kind_snapshots）、supervisor `Ezagent.Workspace.Supervisor`（:69）。
- `Ezagent.ActionSet.Workspace`（behavior/workspace.ex:153，913 行）— `use Ezagent.Lifecycle`。**14 个 action**（:174-298）：list/add/remove_member、assign/unassign_role、list/add/remove_template、list/set_routing_rules、instantiate、create_agent、create_session、remove_cross_prefix_members。`create/1`（:319，从 Loader-threaded args 建 slice，NO activate）、`data_owner/1 → :any`、手写 `required_caps/0`（:353，把每 cap 钉到 `:workspace` kind axis 覆盖宏默认）。`handle_instantiate/2`（:763）返 children 作 DATA（`{:member,uri}` 先、`{:template,name,data}` 后）。add/remove_member 带 `:create_session` cap grant（async `:dispatch`）/revoke（sync `:dispatch_returning`）副作用。**注意 `:create_user` 不在此 ActionSet**（在 `WorkspaceUserAdmin`，dispatch target `:workspace_user_admin`）。
- `…Workspace.Members`（members.ex）— read_members/1、`validate_member_prefix/2`（workspace-prefix 不变式 :25）、ensure_member_kind_spawned/1（只 user 预 spawn）。`…AgentCreate`（agent_create.ex，975 行）— `:create_agent` provisioning：coerce→validate→per-flavor 分派（cc/cc-headless/codex/codex-remote 走 file-flavor cascade #17 chokepoint；py 走非 cascade 模板路；curl/native/future 走 `direct_spawn_flavor_agent` :476 + RoleStep）。`…AgentCreate.RoleStep`（role_step.ex，RF-5a direct-spawn 专属：`resolve/2` recipe×flavor Compose、`mint_and_grant_caps/4` CapMint × cap_policy fail-closed、durable marker `grant_recipe_marker/2` 写 `AgentRecipeAttributes` ETS + `:sandbox.:recipe`；file-flavor 带 role → fail-loud RF-5b）。
- `Ezagent.Workspace`（facade，workspace.ex，987 行）— spawn/create/durable mutations（cap-checked）+ 统一 provisioning `create_agent/3`(:768)/`create_session/3`(:818)/`create_user/3`(:888)。dispatch-first persistence（CRIT-1）；#154 `workspace_self_ctx/2`（:630，程序化 mutation 在 workspace 自身 URI + 自 cap INLINE）。`Workspace.Loader`（loader.ex，boot 重 spawn，agent-flavor registry 未 sealed 则跳，`:test` DB 错 fail-loud）；`Store`（Ecto `workspaces`，JSON-text 列，`create/2` unique-insert 作 create-vs-adopt 信号）；`Listing`（cap-derived 可见性）；`MagicLinkRule`（per-workspace domain/user_list/invite_only）；`ResponsibilityAssignment`（P9 supervisor pool，`supervisor_caps/1` 6-cap bundle :122）。
- **易混淆点**：`Ezagent.ActionSet.Workspace`（dispatch 侧）≠ `Ezagent.UI.SessionView`（UI 侧）；Kind `:ephemeral`（`create/1`-from-args 每次重建 SoT-derived state，无 activate reconcile）；`:instantiate` 返 children 作 DATA（plugin-isolation north star）；`session_templates` 列名误导（存 spawn-template 注册 cc.agent/py.agent，非 Phase-7 SessionTemplate Kind）；跨 app 运行时 DI（避 dep cycle，session/agent-spawn facade 经 `Application.get_env`）；**无 Definition/socialware 代码**（在 session），只 RoleStep 消费 `RecipeRegistry`。
- **自 de4f40a5**：#1185（唯一触及生产：role_step.ex + agent_create.ex 把 agent-level marker `:role`→`:recipe`；`grant_role_marker`→`grant_recipe_marker`、`AgentRoleAttributes`→`AgentRecipeAttributes`）；其余 test-only。
- **测试**：20 个（behavior/entity/workspace/integration ×11/mix/tasks）。

---

## L3 plugin

### ezagent_plugin_cc（plugin 层）

- **entry**：`apps/ezagent_plugin_cc/lib/ezagent/plugin_cc/application.ex:72`（`start/2 → Ezagent.Plugin.boot`）。`after_boot/0`（:155）：McpRegistry.init + LiveJoinRegistry.init（:163-164）→ reap orphans（PTY + cc-sdk）→ `Workspace.Loader.load_all` → `CcOrchestratorSeed.seed`（:178）。
- **职责**：统一 Claude Code agent 插件——声明 `cc`（PTY/TUI）+ `cc-headless`（Python SDK sidecar）两 flavor（都挂共享 `Ezagent.Entity.Agent` Kind）、其 Template Class、bridge adapter、内置 `orchestrator` role recipe，host 编排器 MCP transport（McpSocket/McpChannel/McpServer/McpRegistry）。**声明 NO 自有 Kind/ActionSet**（Agent Kind + `ActionSet.CcHeadlessAgent` 在 domain_agent；cc-ness = `entity://agent/<ws>/cc_<name>` 名前缀）。
- `EzagentPluginCc.Application` — `template_classes`=[CcAgent, CcHeadlessAgent]（:93-97）、`agent_flavors`=cc + cc-headless（:100-119）、`roles/0`=[OrchestratorRecipe.recipe]（:130）、`behaviors/0` 由 `CcHeadlessAgent.actions()` 展开成 `{Entity.Agent, action, CcHeadlessBehavior}`（:87-90）。
- `Ezagent.PluginCc.Template.CcAgent`（961 行）— cc(PTY) Template Class，`config_dir_namespace "cc"`（:200）；CredentialAdapter `credential_env_var "CLAUDE_CONFIG_DIR"`（:206）、`credential_relpaths [".credentials.json"]`、`auth_failure_signals`（:220）、`credential_status/2`（:237，**#160 新**）；`instantiate/3`（:403）幂等短路（Kind + PtyServer alive → fresh?:false）否则委派 Spawn。**`check_role`（cc_agent.ex:370-389，T4 2026-06-28）已泛化为接受任何已注册 role**（`RecipeRegistry.lookup`，旧 moduledoc 说 :352 只 default/orchestrator 是 **STALE**）。
- `…CcAgent.Spawn`（cc_agent/spawn.ex，638 行）— PR-3T 抽出的 spawn/PTY/#17 grant-cascade。`spawn_for_local_pty/3`（:64）：`:already_started` 只在 `owns_this_agent?`（URI workspace 段匹配 :593）时起 PTY 否则拒 foreign-Kind adoption；`:started` 路径 create_config_dir_with_grant → try_role_bootstrap → **launch 前二次 grant 复验**（:225）→ ensure_pty_server，失败 terminate Kind 全回滚。`ensure_pty_server/3`（:427，#701 chokepoint）先 `require_transport_join`（:458，默认 30s）+ OnboardingBootstrap → SpawnPlan 建 argv → `Ezagent.Domain.Pty.start`。
- `…Template.SpawnPlan`（spawn_plan.ex，229 行，**BUILD-ONLY** #701）— 组装 cc PTY 启动参数 + claude argv 不 launch。`build_claude_cmd/3`（:58）`System.find_executable("claude")` 绝对路径 + argv `--dangerously-skip-permissions --dangerously-load-development-channels server:esr-bridge` + settings-mcp args。creds/env：`base_env` 放 `EZAGENT_AGENT_URI`+`EZAGENT_AGENT_TOKEN`（来自 McpConfigWriter :63-89）；`put_claude_config_dir`（:205）；`maybe_put_orchestrator_recipe_env`（:197，`EZAGENT_AGENT_ROLE=orchestrator`）；`maybe_put_cli_identity_env`（:167，为任何 role-agent build 期 `Entity.Token.mint` 铸 CLI 身份 token 导出 `EZAGENT_USER_TOKEN`+`EZAGENT_ENTITY_URI`，least-priv，mint 失败 fail-closed 不 fallback admin）。
- `…CcHeadlessAgent`（cc_headless_agent.ex，268 行）— `cc-headless` flavor，CredentialAdapter delegate CcAgent（含 #160 credential_status delegate :42）；`spawn_for_headless/3`（:91）→ ensure Kind → grant → revalidate → `ensure_sdk_sidecar`（:199）。`SdkSidecar`（sdk_sidecar.ex，385 行，supervised Python ClaudeSDKClient worker，`Ezagent.Runtime.OsProcess` erlexec，stdin/stdout JSON-line，query/3 默认 120s，runner 优先 uv run）。
- `BridgeAdapter`（bridge_adapter.ex，254 行）— cc flavor adapter，`transport_class :subprocess_ws`（:17），`channel_topic_prefix "agent_bridge:cc:"`；`handle_client_event("reply",...)`（:30）三桶 dispatched/failed/skipped，空 session_uris reject；`dispatch_reply/5`（:131）经 `Invocation.dispatch`（:163，P14）投 session.send least-priv inline cap。`CcHeadlessBridgeAdapter`（`:in_process_sync`，直 call SdkSidecar.query）。
- `McpConfigWriter`（mcp_config_writer.ex，258 行）— 写 claude `--mcp-config` 消费的 `.mcp.json`。`write_with_token!/1`（:102）幂等 mint per-instance token 并**返回**（不写进共享 mcp.json env 避 clobber，身份改走 cmd_env）；写 4 处。`OrphanReaper`（靠 pid-file 非 ps，start-time 匹配防 PID 复用）；`CredentialFreshness`（credential_freshness.ex，155 行，#17(c) 只读 OAuth 新鲜度 preflight，**#1171 加 status/2 :90 + expires_at/1 :107**）；`CredentialRefresh`（仅 TEST/E2E provisioner）。
- **编排器 MCP transport（PR-8/#53）**：`Ezagent.Orchestrator.McpSocket`（/orchestrator_socket，token auth 复用 AgentBridge.TokenStore）、`McpChannel`（topic `orch:bridge:<uri>`，join 断言 topic==token 认证 agent_uri）、`McpServer`（mcp_server.ex，527 行，**纯 transport 零 authority**，`handle_tool_call/3` :329 按 orchestrator URI 查 SessionManager pid → `{:run_tool, tool, args, bridge_token}` :357；bridge token 是唯一 unforgeable authz 门；moduledoc 说 "9 tools" 是 **STALE 实 12**）、`McpServer.ToolCatalog`（tool_catalog.ex，**实际 12 tool** :9-283：add_managed_member/update_member_template/remove_member/define_rule_set_rule/define_prompt_template/define_legend/update_template/save_template_as/migrate_session/list_templates/kb_query/kb_ingest）、`McpRegistry`（CACHE 非真相源）。`OrchestratorRecipe`（orchestrator_recipe.ex，131 行，flavor-agnostic orchestrator role recipe，skills=[ezagent-session-orchestrator]、requested_caps 4 条 Template read/write/instantiate/fork，persona 列 8 tool 非全 12）。`OrchestratorBootstrap`（cc-flavor role loader，T2 泛化非硬编码）；`OnboardingBootstrap`（写 `.claude.json` hasCompletedOnboarding 抑制首启对话框）；`CcOrchestratorSeed`（cc_orchestrator_seed.ex，586 行，seed `template://system/agent/cc-orchestrator`，MCP bridge/schema 安装是 HARD，moduledoc 说 "7-tool" STALE 实 12）。
- **易混淆点**：session 是真 prod compile dep（mix.exs:82）；MCP transport 零 authority；STALE 文档（McpServer/McpChannel/CcOrchestratorSeed moduledoc "7/9 tool" 实 12；OrchestratorBootstrap moduledoc 说 check_role 只 default/orchestrator 已被 T4 泛化 :370；application.ex moduledoc 说 `behaviors/0` 为 `[]` 已过期）。
- **自 de4f40a5**：仅 #1171（credential-status view，全新增无删）：CredentialFreshness status/expires_at、cc_agent credential_status/2、cc_headless delegate。
- **测试**：38 个（e2e scenario_05/33、integration orchestrator_mcp_*、template 层多个、sdk_sidecar_erlexec、mcp_config_writer）。

### ezagent_plugin_codex（plugin 层）

- **entry**：`apps/ezagent_plugin_codex/lib/ezagent/plugin_codex/application.ex:15`。`after_boot/0`（:84）reap codex orphans → load_all → CodexOrchestratorSeed.seed。
- **职责**：spawn Codex agent——`codex`（app-server + 可见 TUI + Python bridge，三子进程）与 `codex-remote`（headless，无 TUI，两子进程）两 flavor（都 map 到共享 `Ezagent.Entity.Agent`），复用 cc 的 `OrchestratorRecipe`，走 codex-native 编排（AgentBridge/SessionManager `run_tool` seam，**无 cc PTY / cc MCP transport**）。**不声明自有 Kind**。
- `EzagentPluginCodex.Application`（103 行）— `template_classes`=[CodexAgent, CodexRemoteAgent]、`agent_flavors` codex + codex-remote、`roles/0`=[OrchestratorRecipe.recipe]（复用 cc）。`…Template.CodexAgent`（760 行）— `config_dir_namespace "codex"`，CredentialAdapter `credential_env_var "CODEX_HOME"`（:33）、`credential_relpaths ["auth.json","config.toml"]`、`secret_relpaths ["auth.json"]`（config.toml 是 config 非 secret）、`credential_status/2`（:63，**#160**，codex 无可读 expiry → present/absent/unreadable）。`spawn_for_codex/3`（:192）ensure Kind → grant → **launch 前 revalidate**（:212）→ `ensure_sidecars`（:261，app_server → 重置 thread-id → bridge_sidecar → 等 bridge 写 thread_id 15s → PTY）。SUN_LEN：`default_app_server_socket_path`（:661）用 URI SHA-256 前 16 hex 做 socket dir slug（避免撑爆 unix socket 104-byte）。`CodexRemoteAgent`（442 行，只起 AppServer + BridgeSidecar 无 PTY）。
- `AppServer`（app_server.ex，per-agent Codex app-server sidecar，erlexec，`codex app-server --listen unix://<sock>`）；`BridgeSidecar`（248 行，per-agent Python bridge，收 `codex_turn` push→转 app-server→回 `reply`，port_env 注入 EZAGENT_AGENT_URI/TOKEN + CODEX_HOME）；`BridgeAdapter`（174 行，`:subprocess_ws`，`deliver/2` push `codex_turn`，`handle_client_event("run_tool",...)` :50 → **直接 call** `Ezagent.Session.SessionManager.run_tool/4` :153，codex-native seam 靠 codex 对 session 的 prod dep）；`CodexRemoteBridgeAdapter`（delegate BridgeAdapter，flavor codex-remote）。`CodexOrchestratorSeed`（161 行，seed `template://system/agent/codex-orchestrator`，sandbox 写 AGENTS.md=persona + `.codex/config.toml`，无 cc 的 HARD MCP-bridge 安装）。`CredentialFreshness`（credential_freshness.ex，55 行，**#1171 全新模块**，`status/2` :36 present→authenticated/缺→missing/坏→unknown，**刻意无 last_refresh 陈旧启发**）；`CredentialRefresh`（#21 TEST/E2E，目录制刷 auth.json+config.toml）。
- **易混淆点**：每 codex agent 起三子进程（codex flavor）；P14 遵守（bridge_adapter.ex:117 session.send、codex_orchestrator_seed.ex:125 template.write）；session 是 prod compile dep（mix.exs:42）；SUN_LEN 用 16-hex slug；**codex 编排 vs cc**——cc 有独立 orchestrator MCP transport，codex 没有（走 shared AgentBridge run_tool → SessionManager.run_tool 直调）。
- **自 de4f40a5**：仅 #1171（credential-status view，全新增）：新文件 CredentialFreshness、codex/codex-remote credential_status/2。
- **测试**：15 个（e2e scenario_06、template 层、socket_path[SUN_LEN]、credential_status/freshness/refresh）。

### ezagent_plugin_curl_agent（plugin / L3）

- **entry**：`apps/ezagent_plugin_curl_agent/lib/ezagent_plugin_curl_agent/application.ex:70`。**自 de4f40a5 无 lib 变化**（#1175 只改 2 个 test）。
- **职责**：连 OpenAI-compatible `/chat/completions`（DeepSeek/OpenAI 等）的 HTTP-API agent；curl-as-flavor fold（PR-6+7）后**无独立 Kind**，flavor `"curl"` WIRING 到统一 `Ezagent.Entity.Agent`。**curl STATE Behavior `Ezagent.ActionSet.CurlAgent` 模块在 domain_agent 不在本 app**（本 app 仅 `alias CurlAgentBehavior` 引用 application.ex:63，verified by absence）。
- `EzagentPluginCurlAgent.Application` — `behaviors/0`（:85，`for action <- CurlAgentBehavior.actions()` × `{AgentKind, action, CurlAgentBehavior}`，绑 [:reset_conversation,:configure,:sync_result]）、`agent_flavors/0`（:109，flavor `"curl"` + BridgeAdapter + `instance_behaviors: &AgentKind.curl_behaviors/0`）、`after_boot/0`（:152，re-run load_all，Decision #112）。
- `…ApiClient`（api_client.ex，`:httpc` OpenAI-compat，**`body_format: :binary`** 避 CJK 乱码 :66，NO streaming/retry/redaction）。`BridgeAdapter`（TRANSPORT 半，`:in_process_sync` :70，**stateless**，`deliver/2` 读 `:curl_agent`+`:api_keys` **SNAPSHOT slice**（`SnapshotStore.latest` 不 `Kind.get_slice` 避 self-deadlock）→ ApiClient → 返 `{:ok,%{content,usage}}` persist nothing）。`…Template`（curl.agent，实现 Kind.Template + UI.Form + `Ezagent.Agent.CredentialSliceAdapter`：`credential_slice/0 → :api_keys`、`materialize_credential_slice/2` 走 grant-cascade；`instantiate/3` spawn 统一 Entity.Agent + `AgentFlavorAttributes.put(uri,"curl")`）。`CurlSnapshotMigration`（forward-only 迁移 pre-fold curl_agent kind_snapshots → 统一 Entity.Agent，`rewrite_cap_axis` `:curl_agent → :agent` deep-walk）+ `mix ezagent.curl.migrate`（经 `Migration.RepoOnly.run/1` 只起 Repo）。
- **易混淆点**：state/transport split（STATE = reparented Behavior persist via `{:set}`；TRANSPORT = stateless BridgeAdapter）；无 definitions/0 / Definition/socialware。
- **测试**：12 个。

### ezagent_plugin_email（plugin）

- **entry**：`lib/ezagent_plugin_email/application.ex:36`。入站 confirm GET 路由在 web 侧（`EmailBindingController`），本 app 无 web 依赖。**自 de4f40a5 无 commit**。
- **职责**：ezagent.chat 的 email 能力——(1) Swoosh send + Cloudflare-Email-Worker HTTP pull 收信 + `mix ezagent.email` CLI；(2) email ExternalMirror `:push` adapter（session chat 镜像成 RFC 5322 threaded 邮件出站 + 入站 poll loop 把回复注回 session）。纯 transport/adapter，**不声明 Definition/recipe/flavor/roles**，非 socialware unit。
- `Application` — `adapters/0`（`{Adapter, Binding}` :65）、`behaviors/0`（EmailAllow 挂 Session :54-58）、`children/0`（`[Ezagent.Email.Inbound]`，`:test` 跳）。`Ezagent.Email`（facade，`send/4` Swoosh RFC 5322 threading + header-injection sanitize；SMTP 路径读 AppSettings smtp_config 经 SmtpOpts）；`Mailer`（`use Swoosh.Mailer`，adapter 按 env）；`Adapter`（`adapter_kind/0 => :push` :70，`target_ownership_check/2` 仅 RFC-5322 格式 sanity，invariant #17 遵守）；`Binding`（`init/1` HARD-CODE `:pending_verification` :69 永不读 caller opts，`publish/2` gate on durable server-owned `InboundBinding.verified?/1` :85，durable ThreadState）。
- `Ezagent.Email.Inbound`（入站 poll loop，email 无 webhook）— loop/bounce guard → 地址解析绑定 → verified gate → SPF/DKIM/DMARC + From-match auth → dedup → **inject 经 `Invocation.dispatch`（P14）** under least-priv principal → 成功才 DELETE（at-least-once）。`InboundBinding`（durable server-owned verification gate，token SHA-256，24h TTL）；`Inbound.Guard`（accept_for_injection? + authenticated? 无 degrade-to-allow）；`Inbound.Principal`（铸 `entity://<ws>/user/email-<id>` + 恰好一个 session.send cap inline，不经 Grant）；`ThreadState`（RFC 5322 threading）；`Verification`（bind-time wrapper，铸 `@ezagent.chat` alias + 发确认链接）；`Inbox`/`Inbox.CFWorker`（CF Email Worker HTTP pull）；`Email.Allow`（cap-only marker `:allow_email`）。
- **易混淆点**：**`#1198` REST Swoosh adapter 不在本 app**（`Ezagent.Mail.EzagentChatAdapter` + `EzagentWeb.Mailer` 在 ezagent_web 层，仅供认证邮件；本 plugin 的 Mailer/send 仍 Swoosh SMTP/Local/Test，未获 REST 分支，两 Mailer 不同 otp_app 勿混）；Binding verification 是 durable server-owned（in-memory `:pending_verification` 只是 forgery-safe 默认非 gate）。
- **测试**：约 15 个测试模块。

### ezagent_plugin_feishu（plugin）

- **entry**：`apps/ezagent_plugin_feishu/lib/ezagent/plugin_feishu/application.ex:70`。HTTP 入站 `webhook_plug.ex:31`，forward 挂本 app 外 `apps/ezagent_web/lib/ezagent_web/router.ex:260`。**自 de4f40a5 仅 #1183 test-only**（生产 lib 无变化）。
- **职责**：Feishu(Lark) 双向 transport adapter——入站（HTTP webhook + WSS long-connect via node sidecar）把 Lark 消息经 `Invocation.dispatch` 注入 session；出站走 generic ExternalMirror（Adapter/Binding 对）；附 open_id↔user 身份绑定 + @mention/@legend 路由消歧 + presence 镜像。不声明自有 Kind。
- `Application` — `behaviors/0`（FeishuAllow 挂 Session + UserBinding 挂 Workspace :98-110）、`adapters/0`（`[{FeishuAdapter, FeishuChatBinding}]` :120）、`children/0`（FeishuChatSupervisor/Client/PresenceMirror/非 test 时 WsClient）、`after_boot/0`（load_all + seed user bindings）。`WebhookPlug`（url_verification challenge 回显 + `schema:"2.0"` event → EventDecoder → InboundDispatcher）；`WsClient`（WSS long-connect，spawn node sidecar `priv/ws_sidecar/main.js` 经 erlexec group-kill，LineBuffer 重组 → InboundDispatcher，5s backoff + OrphanReaper）；`InboundDispatcher`（两入站 transport 共享入口，SenderResolver → pending/bound → InboundChatLookup → `do_dispatch`，**P14** `Invocation.dispatch(inv)` mode: :call 让 cap 拒绝同步冒泡 :99,299，`:unauthorized`/`:cross_workspace_denied`/`:ambiguous_chat_binding` 三种失败各回不同 Feishu 文本+emoji 不 silent drop）。
- `SenderResolver`（Feishu sender → caller+caps）、`InboundChatLookup`（chat_id → session_uri 反查读 generic `external_mirror_bindings` adapter_id `"feishu"`，多绑定 fail-closed 靠 @mention/@legend 收敛到恰好一个 session）、`MentionParser`（`@<name>` grep + KindRegistry 匹配 live agent URI）、`FeishuAdapter`（`@behaviour ExternalMirror.Adapter`，`target_ownership_check/2` 是唯一外部 I/O Lark is_in_chat 探测 10s task）、`FeishuChatBinding`（per-chat 有状态，partial-publish RAISE 防 cursor 越过未发 payload）、`Feishu.Allow`（cap-only marker `:allow_feishu`）、`UserBinding`（挂 Workspace bind/unbind/list，`workspace_scoped?==false` + 内部结构化跨-workspace 检查防 open_id 跨租户劫持）、`PresenceMirror`、`Client`（Lark HTTP，`:httpc`，tenant token cache）。
- **易混淆点**：出站已彻底走 generic ExternalMirror（PR-EM-6 退役单租户 FeishuOutbound + 侧表 SessionBinding）；inbound `send` 声明 `:cast` 但 InboundDispatcher 显式 mode: :call（Decision #134）不 silent drop。
- **测试**：约 16 个测试模块（含 e2e/category_04_feishu_test.exs）。

### ezagent_plugin_hello（plugin / L3，socialware app）

- **entry**：`apps/ezagent_plugin_hello/lib/ezagent_plugin_hello/application.ex:39`。`start/2` boot 时还 `SessionViewRegistry.register(PageView)`（:44）+ `maybe_publish_hello_demo/0`（:59，经真 governance 发 PUBLIC demo，dev/prod fail-loud、:test 跳）。规模 22 lib .ex，3755 LOC。
- **职责**：AI builder agent 把用户 chat 请求变成结构化 `@json-render` UI page（受 shadcn 目录约束），页面经 `Behavior.Turn`→`Behavior.Surface` chokepoint 落地，经 socialware substrate（public_view→ExternalFeed→/socialware/chat）暴露给匿名访客；现由一个**不可见的 per-session orchestrator 前台**按意图×身份把每条消息路由到 page `builder` 或只读 `concierge`。
- `EzagentPluginHello.Application` — `agent_flavors/0`（:96-106，**单一 `"hello"` flavor**，host 到 `Entity.Agent`，template_class HelloAgent，cap_policy 复用 `EzagentPluginNative.CapPolicy.for_recipe/1`）；`roles/0`（:116，**三个 recipe**：`hello.orchestrator`（[HelloOrchestrator]，requested_cap :hello_sync_result）、`hello.builder`（[HelloBuilder]，:receive）、`hello.concierge`（[HelloConcierge]，:receive），三者都是 role×flavor on 统一 Agent 非自有 Kind）；`behaviors/0`（:164-173，唯一静态绑定 `{Session, :hello_render, HelloRender}`）；`template_classes/0`（:88，[Template.HelloSession]）。
- `ActionSet.HelloOrchestrator`（不可见前台，唯一 action `:hello_sync_result` :36-44，`handle_hello_sync_result` :53 交给 `Router.route/3` fire-and-forget，create/1 存 `flavor: "hello"` :47）；`ActionSet.HelloBuilder`（page-gen role，**`from_user?/1` loop-guard :74-75** 用 `URI.type?(uri, :user)` invariant #11，非 user sender→false 不循环，974e6fcd1 只删手写 required_caps）；`ActionSet.HelloConcierge`（只读 Q&A 副驾，`handle_receive` :43 经 `Generator.concierge_start/3`，**结构上无 page-write 路径**——只 builder 能碰 Surface，安全闸）；`ActionSet.HelloRender`（cap-only view read，`dispatchable?/0→false` :39，唯一 action `:hello_render` 避与 kanban 撞）。
- `EzagentPluginHello.App`（hello 编排单一收口）— `ensure_app/3`（:40）seed hello Definition → `Installation.freeze_template_installs`（:54，Decision A 冻结 pin）→ `owner_uri_for_template`（:60，O-1 从 def owner_policy 派生 owner 非硬编码 admin）→ persist template → spawn Session Kind → bind workspace → install installs → ensure_orchestrator。`create_role_agent/4`（:282，RF-5a 经 `Workspace.create_agent` :295，orchestrator 用 `"hello"` flavor 收 chat，builder/concierge 用 `"native"` 从不收 chat）。`seed_hello_definition/2`（:237-260，Definition shape bases[Session, Publisher.SessionImpl]、shape[Turn, Surface]、adapters[external_feed/customer]、visibility_policy{publish_policy::auto, web_anon_access:true}、owner_policy{type::installer}）。
- `EzagentPluginHello.Router`（**非** `Ezagent.Router`，hello 内部路由，**身份优先**：非 owner→**永远 concierge** :52 无 LLM 调用 page-edit 边界结构性；owner→`Generator.classify_intent` :53；owner?/2 :63 读 owner_uri，ownerless→fail-open 当 owner :66）；`BridgeAdapter`（`"hello"` flavor adapter，`transport_class :in_process_sync`，deliver/2 同步返回供 Agent.Receive 再派发进 orchestrator 的 :hello_sync_result）；`Generator`（893 LOC，off-process LLM 生成引擎，generate/build_spec/apply_patch/concierge_answer/classify_intent，theme 二次 LLM，CJK 经 gettext CjkLiteralGate，LLM backend `HELLO_LLM_BACKEND=claude_code` 或 DeepSeek HTTP）；`TurnDriver`（Turn chokepoint，drive/4 顺序 turn.open→compose→settle 落 page 到 Surface）；`PageView`（内部 SessionView，`view_behavior/0`=HelloRender cap-gated，`applies_to?/1` 认 hello type OR **manifest-installed hello** manifest_installed_hello? :55 经 `Installation.installed_definitions`，#1164 消费侧）；`Migrate`（一次性给旧 hello session 补前台）；`Template.HelloSession`（session.hello，instantiate 委托 App.ensure_app defer_orchestrator）；`Template.HelloAgent`（`"hello"` flavor minimal Template Class）；其余 Spec/Sanitize/ShellCss/Prompts/LLM.ApiClient/LLM.ClaudeCode/Gettext。
- **易混淆点**：**三角色模型**（chat 只 fan-out 到 orchestrator 唯一 chat member；builder/concierge 不作为 chat member join，仅作回复署名身份，orchestrator 经 admin-authority TurnDriver 驱动，防双重动作）；`"hello"` flavor 只给 orchestrator，builder/concierge 用 native（native 无 adapter→chat 被丢）；Definition 非静态 seed（App.ensure_app imperative + `DefinitionRegistry.seed_definition_if_absent`；publish 走 governance `Demo.Hello` 模块在 **domain_session**）；mix deps 含 ezagent_plugin_native（保 native flavor 在 hello app tree 启动）。
- **自 de4f40a5（3 commit）**：#1168（最大——删 `entity/hello_builder.ex` -39 旧 Kind 声明，builder 从"自有 Kind"改成统一 Entity.Agent 上的 role，新增 HelloOrchestrator/HelloConcierge/BridgeAdapter/Router/Migrate/Template.HelloAgent；**旧报告"HelloBuilder 是唯一 session 成员/无 concierge-orchestrator 层/有 Entity.HelloBuilder Kind"全作废**）、#1173（app.ex 插 freeze_template_installs + owner_uri_for_template）、#1180（role-slot P1 小收敛）。
- **测试**：8 个（generator/registration/router/spec/template/hello_session + integration hello_freeze_pin/hello_orchestrator_delivery/hello_page_e2e）。

### ezagent_plugin_kanban（plugin / L3，kanban-as-role）

- **entry**：`apps/ezagent_plugin_kanban/lib/ezagent_plugin_kanban/application.ex:32`。规模 11 lib .ex，2493 LOC。
- **职责**：node-tree（思维导图）看板 Behavior + 出站连接器（GitHub/Miro/markmap）；board **不是** `resource://` Kind——是 role `kanban-manager` × flavor `native` 的 passive agent，board state 住通用 `Entity.Agent` 的 `:kanban` snapshot slice（真相源）；纯声明式路 A 插件。
- `Application` — **无 `kinds/0` / 无 `definitions/0`**（K5 删了独立 `resource://` Kanban Kind，`resource_kind_as_genserver` AST gate 永久锁死重引入 :7-9/:44-48）。`roles/0`（:64，[kanban_manager_recipe()]）。`kanban_manager_recipe/0`（:88-105）：`passive: true`（:91，RF-6 三闸：不可@、无:join、无 chat）、behaviors=[`ActionSet.Kanban`]（:92，**仅** Kanban；Connectors 不是 Behavior）、requested_caps 每 action 一个 cap-template（从 `Kanban.actions()` 派生）、**`config`（:99-103）= 9-stage 链 `[:positioning,:metric,:pain,:anchor,:ux,:feature,:issue,:test,:pr]` + `ci_stage::pr` + `import_default_stage::feature`**（LAYER-2 recipe DATA，非 baked 进 Behavior）。`config_surface/0`（:133，`%{kind: :route, path: "/plugins/kanban", label: "看板"}`）。
- `Ezagent.ActionSet.Kanban`（kanban.ex，802 LOC，`use Ezagent.Lifecycle`）— **24 个 action**：15 core（:47-165，add_node/rename_node/move_node/remove_node/set_stage/claim_node/unclaim_node/set_status/attach_artifact/detach_artifact/set_metric/drop_subtree/get_tree/export_markmap/import_markmap）+ 9 connector（:174-244，sync_github/push_pr/register_pr/attach_code_file/sync_prs/sync_miro/set_board_config/save_github_creds/save_miro_creds，handle_* 都薄转发给 Connectors）。`required_caps/0`（:252-281，24 action 每个 `cap(:any, __MODULE__, a)`，kind 轴 `:any` 运行时按宿主 type_name 替换成 `:agent`）。stage R1 单调链（stage_fits?/4 :453 非根只能"父棒"或"父棒+1"）。不变式 `owner==nil ⟺ status==:unassigned`（handle_set_status :515 must_claim_first）。`data_owner(_)→:no_owner`（:285）。
- `…Kanban.Connectors`（connectors.ex，345 LOC，9 出站 handler 实现体，节点级 `Shared.owner_or_admin?`、图级任意持 cap 成员、凭证保存 admin-gated，出站 HTTP 同步 in-process）；`…Kanban.Shared`（shared.ex，190 LOC，**唯一 `{:set, :tree, …}` 收口点** `commit/1` :149；`stages/1`/`config_get/3` read-through over RecipeRegistry：生产经 `ctx[:self_uri]`→`AgentRecipeAttributes.fetch` :123 / `AgentRecipeResolver.recipe_from_durable_snapshot` :125 拿 role 名→lookup config，测试 ctx 直注，无 recipe→退 `[]`）；`Github`（github.ex，131 LOC，`:httpc`+Jason `body_format: :binary`，凭证 `system://credentials/github.yaml`，**纯出站**无 inbound webhook）；`Miro`（201，对称 Github，无 update→delete+create）；`MiroSync`（miro_sync.ex，185，**plugin 自有轮询 GenServer** 不复用 session 锁死的 external_mirror 域，每 tick 入站 GET Miro→Sync.detect_inbound→`dispatch add_node` 回 ezagent P14，出站重读 ezagent 树→Sync.sync_out）；`Miro.Sync`（miro/sync.ex，185，**纯函数出站逻辑**无 GenServer，与 MiroSync 是两模块）；`BoardConfig`（每图连接器配置）；`Ci`（PR gate 纯函数）；`Markmap`。
- **易混淆点**：纯 kanban-as-role（路 A），#1140/#1164 socialware/manifest track **未触及** kanban；两个 Miro 模块别混；stage 链是 recipe config DATA（Behavior 层零具体棒名）；唯一写收口 `Shared.commit/1`；per-node 授权在 handler 内查（`ctx.caller == node.owner` 或 wildcard admin）；P14（inbound Miro 经 `Invocation.dispatch`）。
- **自 de4f40a5（1 commit）**：#1185（仅 shared.ex：`AgentRoleAttributes`→`AgentRecipeAttributes`、`AgentRoleResolver`→`AgentRecipeResolver`，把 role-provenance 读点重指到 recipe-provenance，**行为语义不变**）。
- **测试**：8 个（behavior/kanban/ci/markmap/kanban_role/miro/sync + e2e miro_live/role_native_create/role_native_dispatch）。

### ezagent_plugin_kb（plugin / L3，kb-as-role，path A）

- **entry**：`apps/ezagent_plugin_kb/lib/ezagent_plugin_kb/application.ex:36`。**自 de4f40a5 无 lib 变化**（两 commit 只改 test）。
- **职责**：retrieval-first KB 作为纯插件——一 KB = 一 agent（recipe `kb` × flavor `native`，仿 kanban-as-role），**非新 Kind/新 domain，ezagent Postgres 零行**，corpus+FTS5 全在独立 per-KB sqlite 文件。
- `Application` — `roles/0`（:80，kb_recipe/0：`passive: true` + behaviors `[ActionSet.Kb]` + requested_caps 每 action 一 cap-template `:query`/`:ingest`）、`resource_types/0`（:100，`kb-store`/`kb-source`，authority `&kb_authority/2` :119 R-3 workspace 隔离）、`config_surface/0`（route `/plugins/kb`）。**无 kinds/0，无 definitions/0**。`Ezagent.ActionSet.Kb`（`use Ezagent.Lifecycle` :38，2 cap-distinct action `:query`（READ，FTS5 MATCH top-k）/`:ingest`（WRITE，resolve source→chunk→REPLACE），cap kind 轴 `:any`；**sqlite 连接 TRANSIENT**：`activate/2` :86 从 kb-agent 身份派生 `resource://<ws>/kb-store/<agent>`→FsResolver→开文件（store-path confinement never caller input），`deactivate/3` 关连接，source 读 symlink escape 拒绝 :196）。`Kb.Chunker`（按 grapheme 切，size 800/overlap 100 不切 multibyte zh mid-codepoint）；`Store`（**`exqlite` 直接**非第 2 Ecto.Repo，FTS5 virtual table `chunks` **tokenize='trigram'** 双语 EN/zh，`journal_mode=DELETE` 单文件可移植，每值 BOUND param 防注入）。
- **易混淆点**：每 KB corpus 全在 `<Home>/kb-stores/<ws>/<agent>/kb.sqlite`，ezagent PG 零行零 migration；本 app **HOSTS 两个非-KB E2E gate**（`e2e/plugin_package_codex_gate_test.exs` + `e2e/socialware_p10_codex_gate_test.exs`，反映 plugin-package/socialware track）。
- **测试**：6 个（store/kb_isolation + e2e ×4）。

### ezagent_plugin_native（plugin / L3，native flavor，RF-8）

- **entry**：`apps/ezagent_plugin_native/lib/ezagent_plugin_native/application.ex:67`。**自 de4f40a5 无 commit**。
- **职责**：`native` flavor——通用 NO-engine / NO-sidecar / NO-bridge host for ROLE agent（如 kanban-manager / kb），behaviors+caps+sandbox 由 ROLE recipe per-instance 提供（RF-5a）。
- `Application` — `agent_flavors/0`（:87，flavor `"native"` → `{Entity.Agent, NativeTemplate}` + `cap_policy: &CapPolicy.for_recipe/1`）。**刻意声明 NO `behaviors/0` / NO `bridge_adapter` / NO `instance_behaviors` thunk**（省略 vs `fn -> [] end`：让 native 完全不进 `Entity.Agent.behaviors/0` union，保证 native 实例永不继承 sibling flavor 的 behavior）。`…Template`（native.agent，`config_schema/0 → []`，`instantiate/3` `AgentFlavorAttributes.put(uri,"native")` + spawn `Entity.Agent` **无显式 `:behaviors`**→ base Agent 集，role 层 per-instance 叠加）；`CapPolicy`（for_recipe/1 :71 fail-closed CapMint，closes over recipe `requested_caps` 只对匹配 `{behavior,action}` 返 true，空 recipe 铸零 cap，string/atom key+value 都容忍，**与 py 的 CapPolicy 逐行同**）。
- **易混淆点**：native 是"加 flavor = 加 plugin"故自成 OTP app 虽无引擎；非 socialware 面（间接：native 是 socialware Definition ROLE agent spawn 的通用 host，也是 kb/kanban role 插件的 flavor 宿主）。
- **测试**：2 个（cap_policy + integration/native_flavor_spawn）。

### ezagent_plugin_protocol_api（plugin，inbound transport adapter）

- **entry**：`lib/ezagent_plugin_protocol_api/application.ex:23`（`after_boot/0` init PendingReplyStore ETS）。HTTP 入口 `open_ai/chat_completions_plug.ex:40`，forward 挂本 app 外 `apps/ezagent_web/.../router.ex:263`。**自 de4f40a5 无 commit**。
- **职责**：inbound OpenAI/Anthropic-compat HTTP API——今实现 OpenAI `POST /v1/chat/completions`，ack-then-async-reply（POST 返 202 + retrieve_url，client 轮询 GET）；durable API-key 认证 + `conversation_id→session` 绑定。**声明 NO Kind/ActionSet/recipe/flavor/Template**。
- `OpenAI.ChatCompletionsPlug`（#96 从 OpenaiChatPlug 改名）— POST 管线 RequestNormalizer → ApiKeyStore.verify → extract_conversation_id → ConversationRegistry.resolve → ensure_session_live → join_agent → build_message → dispatch_send → put_pending + ReplyTransport.spawn_waiter → 202。**两个 latent smell 仍在**：`join_agent` 里 `wait_for_kind_registry` poll + **`Process.sleep(3000)`**（:182-183）；join/send dispatch ctx **caps 空** `MapSet.new()`（:191,:260）。**P14**：join 走 `Router.dispatch` :196，send 走 `Invocation.dispatch` :263。
- `ApiKeyStore`（**真 durable** Ecto `protocol_api_keys` PK key_id，token `pk_<key_id>_<secret>` Bcrypt 校验，返 `{entity_uri, workspace_uri, target_agent, MapSet.new()}`）；`ConversationRegistry`（durable `conversation_id↔session`，**复用 `external_mirror_bindings`** adapter_id `"protocol_api"`，多行 `:ambiguous_conversation_binding`，**nil conversation_id → stateless ephemeral session**）；`RequestNormalizer`/`ReplyTransport`（spawn_waiter Task subscribe_publisher → ReplyWaiter.wait_for_reply → 渲染入 PendingReplyStore，120s deadline）/`ReplyWaiter`（按 `ref_id==request_id` AND `sender==target_agent` 匹配 Publisher event）/`PendingReplyStore`（ETS）/`ResponseRenderer`（usage 全 0）；`Adapter`（`adapter_kind/0 => :request_scoped` 无 Worker，`cap_subject/0` **behavior_module: nil** P0 stub "API-key auth 在 cap model 之外"）；`Binding`（**P0 no-op stub**，仅满足 Grill-5）。
- **易混淆点**：认证是 **API-KEY 刻意在 CapBAC 之外**（Adapter cap_subject nil stub，dispatch ctx caps 恒空，但**现有真 durable ApiKeyStore** bcrypt 表，非纯 stub）；adapters/0 只为满足 Grill-5 契约（真流程在 plug + ReplyTransport/ReplyWaiter）；默认 agent `py_default`（stored flavor `py`，非 URI 名前缀派生）。
- **测试**：约 4 个测试模块（conversation_registry/reply_waiter/default_agent/chat_completions_plug_integration）。

### ezagent_plugin_py（plugin / L3，py flavor，terminal python host）

- **entry**：`apps/ezagent_plugin_py/lib/ezagent_plugin_py/application.ex:76`。**自 de4f40a5 无 lib 变化**（#1203 修复落在 domain_agent 的 AgentFlavorResolver）。
- **职责**：`py` agent flavor——每 py-agent 跑 operator 提供的 python script 在 per-agent `Domain.Python` 子进程，一 JSON-RPC `"receive"` per inbound；folded 到统一 `Entity.Agent`（curl 先例，**声明 NO 自有 Kind**）。
- `Application` — `behaviors/0`（:93，`{Entity.Agent, action}` × `ActionSet.PyAgent` 三 action）、`agent_flavors/0`（:103，flavor `"py"` + bridge_adapter + `cap_policy: &CapPolicy.for_recipe/1`）、`roles/0`（:129，np recipe）、`seed_default/0`（:222，seed `entity://system/agent/py_default` 跑 echo.py）。np recipe（:149，**只 name + script，无 requested_caps**，empty recipe 铸零 cap）。`Ezagent.ActionSet.PyAgent`（STATE 半，`use Ezagent.Lifecycle`，3 py-namespaced action `:py_sync_result`(cast)/`:py_reset`(call)/`:py_configure`(call)；`activate/2` :272 rebuild phase 订阅 + 从 `state.script_path` re-spawn 子进程；reply 走 inline narrow `session.send` cap #154）；`Ezagent.Template.PyAgent`（py.agent，**非-credentialled YET config-dir-allocating** `config_dir_namespace/0 → "py"` :75，MUST NOT 是 CredentialAdapter；`instantiate/3` 写 operator script 到 `<config_dir>/agent.py` → spawn Agent Kind → start 子进程；**script 不可变门 `install_script/2` :232** 二次写不同内容 → `:script_immutable`；**subprocess-liveness 与 materialization 解耦**：子进程 start 失败不回滚 materialize，Kind 留 DEGRADED，下次 :receive 出 `:not_alive`）；`BridgeAdapter`（TRANSPORT 半，`:in_process_sync`，deliver/2 经 `Domain.Python.call(handle,"receive",...)` persist nothing）；`CapPolicy`（for_recipe/1 fail-closed，结构与 native 同）；`OrphanReaper`（namespace `"python"`）。
- **易混淆点**：py 是非-credentialled 但 config-dir-allocating 的新 flavor 形状；`np` 是 py-ROLE（py flavor + re-homed `priv/python/np.py`，空 requested_caps）；`py_default` 跑 `priv/python/echo.py`（echo 退役进 py，被 protocol_api default-agent + web home 消费）；socialware 无关（间接：socialware Definition 的 agent 可指 flavor "py"）。
- **测试**：7 个（py_create_e2e/np_role/py_security/seed_default/echo_equivalent_session + behavior + template）。

### ezagent_plugin_world（plugin — web entry `:ezagent_web_world`）

- **entry**：`apps/ezagent_plugin_world/lib/ezagent_plugin_world/application.ex:14`（`start/2` → `Ezagent.Plugin.boot/1`）。LiveView shell `world_live.ex`（977 行）。
- **职责**：坐在所有 plugin 之上的 React/shadcn UI HOST——一个 LiveView SSR/comms shell（WorldLive）把左-rail nav、sessions/identities/workspaces/admin/kanban 路由、per-session conversation 渲成 React island，own workspace-scoped layout 持久 + socialware author→publish→install→use 的 world 侧面 + world-side plugin-UI-surface 契约；声明 **NO Kind / NO flavor / NO recipe**，只有一个 ActionSet（`Behavior.Layout`）+ 一个 resource_type（`world-layouts`）。
- `EzagentPluginWorld.Application` — `behaviors/0 = [{Ezagent.Entity.Workspace, :manage, Ezagent.World.Behavior.Layout}]`（:41-46）；`resource_types/0` 声明 `"world-layouts"` + `world_layout_authority/2`（uri.<ws> == scope.workspace R-3 非同源守卫 :58-92）；`after_boot/0 → LayoutBootstrap.ensure_system_admin_manage_cap/0`。**新增**：`start/2` 现调 `register_session_views/0`（:16,25-29）——`SessionViewRegistry.init/0` + `register(ConversationView)`，让 chat 成为 registry 里的一等 view（#1199）。
- `EzagentPluginWorld.WorldLive`（977 行，逼近 arch cap）— SSR/comms shell。`mount/2` 出 layout_json/plugin_nav_json/caller_json/world_state_json（:60,23-75）；`handle_params/3` 经 `Routes.route_for/2` → `state_for_route/3` 分派各 *Data 模块（:78-660）；`handle_event("world:dispatch",…)` 按 action 前缀 whitelist 委派 6 个 *Actions 模块（:225-273）；`handle_info` 收 audit/authz/cc/pty/chat_message/member_*/read_marker/notification/slice_changed → push_inbound_event（:120-193）。`socialware_rows/1`（:751-813，含 roles + recipe→agent_options）；`session_template_names/1`（:696-734，directly_creatable? 过滤 + "default" 领头）。
- `Ezagent.World.Routes`（纯 URL→route，含 `/plugins/kanban[/<uri>]` :108-128、`/overview`、`/admin/sessions/<uri>/external_mirror`）；`Navigation`（world:navigate push_patch allowlist，拒 `#`/`://`/`//` 外链）；`ConversationActions`（1090 行，socket-side 会话 dispatch，`handle_dispatch/3` 覆盖 chat.send/session.switch/invite/remove_participant/**assign_role**/create/fork_config/publish_template/view.switch/pty.open/orchestrator.restart/turn.claim/settle/surface.approve/supervisor.verdict/routing.*，`create_session/6` 带 `socialware_ref`+`revision`（content-hash #O-1）+`install_config`（**role_slots**）→ `SocialwareInstall.prepare_create_template/6`，`publish_template/3` = 会话发布为可复用 SessionTemplate via `Orchestrator.Tools.Templates.save_template_as`，`assign_role/4`→`Session.RoleAssignments.assign_role`，`verify_grants` Phoenix.Token 反洗）；`ConversationData`（808 行，`state_for/2` 出 island state 含 `"views"` **registry-driven tabs** `session_views/2`=`SessionViewRegistry.applicable_views/2` caller-aware world 只 classify render mode ∈ chat/pty/external/unsupported，`session_view_ids/2`=switch_view 白名单单源，`parse_mentions/2`→bare @name 先按 URI path segment 再 display_name 唯一匹配才解析，`human_role_slots/1` 从 `Installation.installed_definitions` 取 human roles，`read_unfiltered?` 按 cap 决定 recent_in_session vs recent_visible）；`ConversationView`（world-own `@behaviour Ezagent.UI.SessionView`，id :conversation、`view_behavior/0 → nil` 不 cap-gate、`applies_to?(%URI{})→true`，HEEx render 是 stub 实际用 React Conversation.tsx，**新文件 #1199**）；`ConversationSessionState`（switch_session/2 就地切不 route-remount）；`SocialwareInstall`（210 行，`prepare_create_template/6` nil/empty ref 原样返回，真 ref 则 resolve_install_entry 校验可见+revision-pin → put_install_config 嵌 role_slots → 写本地 install SessionTemplate `%{installs:[entry]}` + TemplateTags "current"，`resolve_install_entry` content-hash-addressed 经 `DefinitionRegistry.resolve_installable_revision/3` scope-gated，install 写用新 mint session_template `:within_workspace` write cap granted_by caller 无 admin fallback）。
- `WorkspacePluginActions`（443 行，`save_session_template/2`=**author→publish 全链**：`prepare_form_socialware`→`DefinitionEditor.validate_definition(socialware, complete:true)` → `save_prepared_socialware`→`DefinitionEditor.save_authored_definition` → `SessionTemplate.create` 嵌 socialware_ref → `publish_current_template` 标 "current"；**#165 caps 穿线** `caps = Map.get(socket.assigns, :current_caps, …)` 现真实携带并传进 save_prepared_socialware 让 domain 侧 public-scope admin gate 授权 `:public` publish :195-209 注释标 SECURITY #165）；`WorkspacePluginData`（656 行，`plugin_nav_surfaces/0` Layer-2 左-rail 枚举 `PluginRegistry.list_all` 里 `nav_surfaces/0` duck-type + fail-closed，`plugin_session_tabs/1` Layer-3 per-session tab `session_tabs/0`+`:condition` 谓词，`socialware_web_anon_access?/1` 读 SYSTEM ws definition fail-closed）；`UiSurfaceProvider`（**World-side 非 core** plugin-UI-surface `@behaviour` nav_surfaces/0+session_tabs/0 `@optional_callbacks` decision 2026-06-30 duck-typed 避 plugin→world 编译反向依赖）。
- `KanbanActions`（406 行，kanban-as-role K4 纯 dispatcher，所有节点动作经 `URI.with_action(entity://<ws>/agent/<id>, :kanban, action)` dispatch，`create_kanban`=`Workspace.create_agent`（role `kanban-manager` × flavor `native` RF-5a 不再合成 `resource://kanban`），dormant agent 经 `KanbanData.ensure_spawned/1` rehydrate）；`KanbanData`（315 行，`list_instances`=`AgentRecipeResolver.list_by_recipe("kanban-manager", workspace_scope)` RF-7 覆盖 live+dormant，`board_snapshot/2` 一次 `:get_tree` dispatch，`stages_from_recipe`=read-through `RecipeRegistry.lookup("kanban-manager").config.stages`）；`IdentityData`（913 行最大读模型，identities/users/agents table + agent_detail + agent_new_form + entity_caps + pty_terminal，`put_credential_statuses` **#160** 经 cap-gated non-activating `Domain.Agent.read_credential_status/2` 注入 co-tenant 学不到）；`AgentActions`（delete 前跑 authz gate non-activating 防跨租户泄漏）；`AdminActions`（admin.smtp.* + external_mirror.bind/unbind 经 `ExternalMirror.bind/5` facade）；`AdminData`（read-heavy Ecto 按 workspace_uri scoped）；`LayoutManager`（file-backed，bytes 经 `resource://<ws>/world-layouts/<name>` + FsResolver，R-3 caller scope 独立）；`SlotRegistry`（world typed-slot 唯一真源，生成 checked-in slots.manifest.json）；`Behavior.Layout`（唯一 ActionSet，`use Ezagent.Lifecycle, state_slice: :world_layout`，action :manage，mount 到 Workspace :manage，R-3 caller scope 从 ctx.caller 独立构造）；`LayoutBootstrap`（after_boot 授 system admin `world.layout.manage`）；其余小模块 SessionForkAction/ConversationRoutingForm/CredentialCascade/CapData/CommandPalette*/User*/CallerDisplay/Jsonable/mix World.Slots.Manifest。
- **易混淆点**：**socialware lifecycle CORE 不在 world**（manifest_resolver/definition_registry/config_governance/definition_agents 全在 domain_session；world 只 CONSUME DefinitionRegistry/DefinitionEditor/Installation/RoleAssignments）；**两条 SessionView 消费路径易混**——`ConversationData.session_views/2`（走 core `SessionViewRegistry.applicable_views/2` caller-aware 含 cap 门，chat/pty/hello-page）vs `WorkspacePluginData.plugin_session_tabs/1`（Layer-3 world-side plugin duck-type tab，kanban）；switch_view 无绕过（白名单严格取 `session_view_ids/2`，cap-gated view 既不出 tab 也不可切，唯一例外 switch_to_pty 硬连 "pty"）；plugin→world 无编译依赖（duck-typed `function_exported?`）；moduledoc stale（application.ex:5-7 说 "declares no Kinds… in PR-0" 但 behaviors/0+resource_types/0 已填）。
- **自 de4f40a5（11 commit）**：#1199（world 通用消费 SessionViewRegistry：新 ConversationView + register_session_views + session_views 改 applicable_views/2 caller-aware + switch_view 白名单动态 session_view_ids + render_mode 4-way，React Conversation.tsx 动态 tab）、#1207（本 HEAD，polish conversation user surface，主体 React）、#1128（refine conversation user surface）、#1194（role-slot install：SocialwareInstall install_config/role_slots + ConversationActions session.assign_role + create_session install_config + ConversationData human_role_slots + world_live socialware role rows recipe→agent_options）、#1185（role-slot P2：`socialware_role_rows` 用 `recipe:` 非 `role:` world_live.ex:776——**skill 需同步的 API 改名**）、#165（save_session_template 穿 caller 真实 caps 给 public-scope gate）、#160（credential-status view）、#161-C（admission gate）、#1176（content-hash install revision-pin）、#1168（world 侧 ensure_hello_orchestrator 复用）。
- **测试**：40 个（含 conversation_view_test #1199、view_cap_gate_regression、save_session_template_authored_pin/public_scope_gate #165、socialware_install、kanban_data、admission_gate_world_invite #161-C、plugin_nav_surfaces/session_tabs、slot_registry/slot_mount_gate、layout_manager/migrate_layouts）。

---

## transport 顶层

### ezagent_cli（top / operator）

- **entry**：客户端 `apps/ezagent_cli/lib/mix/tasks/ezagent.ex:44`（`Mix.Tasks.Ezagent.run/1` → `:rpc.call` :54）→ 服务端 `lib/ezagent_cli/exec.ex:45`（`EzagentCli.Exec.exec/2`）。**自 de4f40a5 无 commit**。
- **职责**：`mix ezagent <kind> <action> [--flags]` 的 THIN 分布式-Erlang RPC shell——客户端连已跑的 runtime BEAM，在那 BEAM 内跑 parse+coerce+dispatch（同 KindRegistry/Repo/audit as LiveView），维持 CLI↔LV runtime 同构。
- `Mix.Tasks.Ezagent` — pluck `--token`/`--uri`（回退 `EZAGENT_USER_TOKEN`/`EZAGENT_ENTITY_URI`）+ `Ezagent.Runtime.connect_as_cli` + RPC，runtime 不可达 → exit 5。`Mix.Tasks.Esr`（**DEPRECATED 薄 alias**，2026-05-26 renamed）。`EzagentCli.Exec`（服务端执行器，**auth MANDATORY**：help/version/no-args 走 unauth，真 argv 走 `exec_with_auth/2` :64，`resolve_caller` 拒 nil/空 token → `:no_token` → exit 4 堵 silent-admin-elevation，token 认证成功塞进 process dict `:ezagent_cli_caller_override`）。`EzagentCli.Dispatch`（建 `%Invocation{}` dispatch P14，`derive_caller` 拒无身份：token override 优先否则 `--as` 需 `EZAGENT_CLI_ALLOW_AS=1`；`build_target_uri` canonical + `?action=<slice>.<action>`，bare-name promotion 需 `--instance-class`）。`TreeBuilder`（走 `BehaviorRegistry.list_all` + `FacadeRegistry` 建 Optimus 子命令树，每次重建）；`Coercion`；`Formatter`（exit code 0/1/2/3/4/5）；`FacadeRegistry`（per-kind facade op ETS）；`Application`（注册两 core facade op `{:workspace,:create}` + `{:agent,:spawn_manifest}`）；`AgentManifestFacade`。
- **易混淆点**：thin-shell RPC（单机假设）；BehaviorRegistry 派生命令树（运行时重建非编译期）；**STALE-NAMING drift 仍在**（CLI 仍消费 `BehaviorRegistry.list_all/0`/`behavior_module.interface()`/`state_slice()`，core 仍存故 CLI 工作只是模块名未 rename）；socialware track 对 CLI = 无影响（CLI 的 "manifest"=AgentManifest 单-agent spawn，与 socialware manifest track 无关）。
- **测试**：8 个（agent_manifest_facade/coercion/facade_registry + 4 integration cli_dispatch/cli_lv_cap_parity/cli_lv_same_server_invariant/plugin_isolation_cli）。

### ezagent_web（top / web-transport，P13）

- **entry**：`apps/ezagent_web/lib/ezagent_web/endpoint.ex:6`（末端 plug `EzagentWeb.Router` at :110）；OTP `application.ex:9`（children Telemetry + Endpoint，先 `RateLimiter.init_table`）。LOC ≈ 6682 行。
- **职责**：整个 umbrella 的 Phoenix HTTP/WebSocket 入口——拥有 endpoint、router、认证边界（login/register/reset/magic-link/email-confirm）、session-principal 写漏斗、LiveView on_mount 认证门、匿名 socialware ingress（cookie+admit+takeover）、socialware pull-feed WS 面、自动派生 JSON API、杂项 transport controller。**声明 ZERO Kind/ActionSet/flavor/recipe**（纯 transport）。World/admin UI 本体不在此（所有 `/admin`、`/identities`、`/workspaces`、`/plugins`、`/sessions` delegate 到 `EzagentPluginWorld.WorldLive`）。
- 路由/端点：`Router`（双 world scope host-scoped + runtime WorldHostScope，都过 RequireEntity；public `:browser` scope 含 HomeLive/auth + **3 条无 RequireEntity 的 socialware 公开路由**）；`Endpoint`（5 socket `/live`/`/agent_bridge`(cc)/`/orchestrator_socket`(MCP)/`/socialware_external_socket`/`/socialware_chat_socket`；CloudflareRemoteIp plug 在 RequestId 前；Plug.Parsers 12MB）；`CommandRoutes`（Router 投影成 CmdK nav）。
- 认证/身份：`Plugs.RequireEntity`（未认证 bounce /login）、`Plugs.WorldHostScope`、`Plugs.CloudflareRemoteIp`（**#1206 新增**，仅当直连 peer 私网时用 CF-Connecting-IP 改写 remote_ip 修 per-IP rate-limit 塌桶）、`SessionPrincipal`（`:current_entity_uri`+`:current_workspace_uri` 唯一授权写入者，`put/3` :80 触发 `AnonTakeover.maybe_takeover`）、`LiveAuth`（on_mount `:require_entity`/`:require_admin`/`:put_locale`/`:cmdk_nav`，WS 重连坏 URI → /login 不 silent fallback admin）、`Plugs.Locale`（en zh_CN）。
- Socialware 匿名接入/feed：`AnonIngress`（签入 principal 优先否则 anon gated on `PublicView.web_anon_access?`，复用 cookie 或 mint fresh，domain 原语 `AnonAdmission`）、`AnonCookie`（签名 visitor cookie，fail-closed）、`AnonTakeover`（post-auth anon→confirmed merge saga，12 步 with 链）、`SessionFeedChannel`（adapter-参数化 feed channel over `:pull` ExternalMirror.Adapter，两交付纪律 × 两 profile，**唯一 `Phoenix.PubSub.subscribe` :140 是 advisory wake-up**，参与式 post 把用户消息 mention 给隐形 orchestrator `orchestrator_uri` :373 → `hello.orch_*` #1168）、`ExternalFeedSocket`/`ChatFeedSocket`（`ChatFeedAuth.verify` 认 caller token 后 join）、`SessionFeedSocket`（**endpoint 尚未挂载 PR-1 additive**）、`FeedEncoding`（`{id,text,sender,render,render_css,nav}`，`nav` #1168 加）。
- Controller：`ApiV1Controller`（自动派生 JSON API，`POST /api/v1/:kind/:action`，`resolve_caller/1` **强制 Bearer + `X-Ezagent-Entity-URI` header 无 admin fallback** PR #123）、`ExternalFeedController`/`ChatFeedController`（外部 SPA shell + 公开附件下载 capability-based）、`SessionController`/`MagicLinkController`/`RegistrationController`/`PasswordResetController`/`EmailBindingController`（认证边界，anti-enumeration + rate-limit）、`UploadsController`/`WorldUploadsController`（签名 DownloadToken + serve-time 参与者复验，授权走 dispatch chokepoint）、`WorkspaceSwitchController`、`CcEventsController`（故意无 auth）、`PluginAssetController`、`HealthController`/`FallbackController`。
- LiveView/邮件：`HomeLive`（根 `/`，**W0 租户隔离 #1150**：landing 判据用 workspace-scoped `list_sessions/1` 修跨租户 existence leak）、`Ezagent.Mail.EzagentChatAdapter`（**#1198 新增**，命名空间 `Ezagent.Mail` 非 EzagentWeb，`email.ezagent.chat` REST Swoosh，`recipients/1` 总是发数组 **#1200 修**）、`RateLimiter`。
- **易混淆点**：匿名在 RequireEntity 外（public socialware 路由刻意在 public `:browser` scope，门是 `PublicView.web_anon_access?` 在 AnonIngress 非 plug，private session bounce /login，router.ex:159/166/173-174/186）；`/api/v1` catch-all 单 controller（真表面在 BehaviorRegistry，无 admin fallback）；**P14 遵守**（web 层 6 dispatch site，0 outbound `PubSub.broadcast`，唯一 subscribe 是 session_feed_channel.ex:140 advisory）；umbrella 聚合点（mix.exs 声明 **19 个 in_umbrella 依赖**，起 ezagent_web = 起每个插件）；此处 SessionFeedChannel 是 Phoenix.Channel 非 CC Channel。
- **自 de4f40a5（12 commit，多数只动测试）**：#1206（新 CloudflareRemoteIp plug）、#1198（新 EzagentChatAdapter REST Swoosh）、#1200（EzagentChatAdapter to 始终数组）、#1205（zh_CN 翻译，lib 无改）、#1168（feed_encoding +nav + session_feed_channel post 改 mention orchestrator）、#1128/#1188/#1199/#1180/#1185/#1176/#1173（全部只动 test，lib 未变）。
- **测试**：42 个（auth controllers、socialware anon_*/feed_channel/socket、plugs cloudflare_remote_ip/locale/require_entity、session_principal、live_auth+presence、api_v1、mail adapter、world_conversation/host_routing、command_routes、rate_limiter、home_live）。
